# NexHub v2.0.0-beta.2（预发布测试版）

> 四合一媒体聚合客户端（动漫 / 漫画 / 小说 / 影视）—— 源即插件 · 共创社区。

---

## ⚠️ 本版说明（预发布 · beta 测试）

这是 **2.0.0 的预发布测试版（beta.2）**，不是正式稳定版。在 beta.1 的基础上，我们继续打磨了**小说 / 漫画 / 影视三大阅读器**的体验，并新增了 **AI 翻译 / 配图、阅读进度云同步、便携文档解析**等能力。它**仍在测试中**，可能有个别未发现的问题。

### 相对 v2.0.0-beta.1 的主要变化（亮点）

- **小说阅读器大升级**：双页模式、整本页码、双语 / 段落翻译（AI）、AI 章节配图、阅读器内正文编辑、墨水屏主题、书源 JS 简繁、压缩包批量导入、Mobi / PDF 解析、阅读进度细粒度 WebDAV 云同步、EPUB 导出模板、划线 / 高亮 / 摘录 / 分享等。
- **漫画阅读器增强**：夜览暖色盖层、睡眠定时、资源 / 内存优化、双击三态、E-Ink 刷新、ICC 校色、自动收藏、预加载 1–16 等。
- **影视 / 播放器增强**：Anime4K 超分辨率、弹幕发送、缓存降级、错误重试、声明式菜单、字幕按视频记忆、防重叠轨道，以及系统画中画 / 投屏的完整生命周期与桌面端画中画。
- **跨类型 / 书架 / 图库**：跨作品待读队列、统一图片收藏图库、书架排序 / 分类筛选。
- **源登录 / 验证**：API Key 登录模式、内嵌 WebView 登录 + Cookie 回灌、Cloudflare 验证修复。
- **大量实机修复**：封面丢失、条漫跨章衔接、加密视频下载、单章进度、本地视频封面、分类冷启动等。

> 底层已在 beta.1 升级到 Flutter 3.47（构建链对齐 Java 17 / AGP 8.11.1 / Gradle 8.14 / Kotlin 2.2.20，最低 Android 仍为 7.0）。

### 已知风险（请知悉）

- beta 版可能仍有个别 bug；部分功能（超分辨率画质 / 弹幕发送 / 投屏 / 画中画）依赖真机或特定环境验证，升级如遇问题请反馈。
- 追求绝对稳定的用户可继续用 v1.2.0，等正式版；想抢先体验的欢迎升级并反馈问题。

### 数据兼容性（好消息）

经代码核查，**本版没有破坏性数据变更**：v1.2.0 / beta.1 的收藏、进度、书签、历史、分组、评分、短评**可直接沿用**；**旧的源 JSON 无需修改、无需重新导入**。

### 升级方式（Android）

本版**沿用与历史版本相同的签名密钥**，可以**直接覆盖安装**，无需卸载。

---

## 🔭 路线规划（规划中 · 欢迎参与）

以下功能**尚未实现**，是接下来几个版本的重点方向。它们的设计同样存在大量取舍，欢迎在 [Discussions](https://github.com/nexhub-app/Nexhub/discussions) 或 Issues 提意见：

1. **漫画翻译（MTL）**：探索漫画图片的机翻嵌字 / 气泡替换，降低跨语言漫画的阅读门槛。
2. **视频实时翻译**：影视模块的字幕 / 实时字幕翻译，支持外挂与内嵌字幕的语言切换。
3. **增加其他同步方式**：在现有 Bangumi 同步之外，接入更多同步后端（如 AniList、MyAnimeList、Trakt、SIMKL、MDList 等），并支持跨后端双向同步与冲突策略可配置。

> 已实现：小说翻译（AI 双语 / 段落翻译）、AI 辅助（内容摘要 / 章节配图）已在 v2.0.0-beta.2 落地；AI 搜源 / 自然语言检索仍在规划中。以上规划项不含在本次发布中；具体落地顺序与形态会以社区反馈调整。

---

## ✨ 本次更新内容（v2.0.0-beta.1 → v2.0.0-beta.2）

> 相对 beta.1 共 107 个提交。以下为面向用户的重点，完整逐条记录见 [RELEASE_NOTES](./RELEASE_NOTES.md)。

### 📚 小说阅读器（大量新功能）
- **双页模式**：宽屏翻页模式下左右并排双页，如摊开的书，进度以左页为准。
- **整本页码**：页眉 / 页脚新增「整本页码」槽位，跨章累计全书页位。
- **双语 / 段落翻译**：接入 AI 翻译（OpenAI 兼容），原文 / 译文对照面板，译文按书缓存；导出时附 `_段落翻译` 附录。
- **AI 章节配图**：根据章节内容生成插图，以插图行插入本章，重载即图文混排。
- **AI 阅读总结 / 内容摘要**：根据全书或章节生成摘要，快速回顾剧情。
- **内容编辑**：阅读器内直接改正文并保存，已编辑章节带角标、可「恢复原文」。
- **墨水屏主题**：选中墨水屏预设时正文炭灰 + 朱批强调色联动，夜间自动回退亮色文字。
- **书源 JS 简繁函数**：源脚本可直接调用 `t2s()` / `s2t()` 做简繁转换（与阅读器正文转换同语义）。
- **压缩包批量导入**：支持 zip / cbz / rar / 7z 归档内 txt / epub 解包入库，自动解析书名 / 作者。
- **便携文档解析**：新增 Mobi / PDF 文本层解析（纯 Dart，无需额外依赖）；UMD 暂无可靠样例，暂明确提示不支持。
- **阅读进度细粒度云同步**：WebDAV 逐书单文件同步，退后台静默上传，冲突自动裁决。
- **EPUB 导出自定义模板 + WebDAV 上传**：自定义 CSS / 封面 / 简介，导出产物可一键上传到 WebDAV。
- **划线 / 高亮 / 摘录 / 分享**：选区高亮 + 摘录笔记 + 文艺分享卡片；支持换源重定位，随 EPUB / TXT 导出附带。
- **朗读通知栏控制**：TTS 通知栏显示播放 / 暂停 / 上句 / 下句。
- **在线多音色 TTS**：自定义 HTTP 模板引擎 + 角色→音色映射，多音色朗读（不内置云端密钥，需自行配置）。
- **阅读中预下载**：读到阈值后后台预下载后续章节，离线可读。
- **平滑自动翻页**：可按间隔平滑推进，滚动推像素 / 翻页推过渡进度。
- **书签角标自定义图**：可为书签设置自定义角标图标。
- **书架未读角标 / 新章提示**；排序按最新章时间 / 中文拼音序。
- **搜索结果繁简转换**；本地书文件名自动解析书名 / 作者。
- **长正文多页并发抓取**：目录多页（nextContentUrl）并发保序拼接，加快大章加载。
- **滚动模式插图样式 / 对齐**：插图支持 banner 铺满或 card 缩列，可选左 / 中 / 右对齐。
- **翻页动画与手势**：6 种动画、下滑切书签手势、音量键翻页。

### 🖼 漫画阅读器（增强 + 优化）
- **夜览暖色盖层**：独立开关 + 强度滑杆，暗光阅读更舒适。
- **睡眠定时**：按分钟（15–90）或按话数（1–3）双模式。
- **资源 / 内存优化**：归档临时目录退出回收、预载 / 渲染窗口淘汰、图片缓存按内存预算 + 磁盘清理入口、退后台冲刷进度防进程被杀、条漫解码限幅。
- **双击三态循环**、**E-Ink 刷新**、**ICC 校色**（6 种色彩预设）、**自动收藏封面**。
- **预加载数量可配（1–16）**、键盘快捷键补全、鼠标滚轮速度可调、鼠标光标反馈、放大态边缘滑动切页、缩放锚点可配、图片加载失败重试。

### ▶ 影视 / 播放器（增强 + 稳定）
- **超分辨率（Anime4K）**：效率 / 质量两档，无清晰度源时提升观感（按需开启，注意性能）。
- **系统画中画完整生命周期**：进入时记录播放位置、退出后续播，小窗三动作（播放 / 暂停、弹幕、快进），不满足条件不进入。
- **投屏完整生命周期**：投屏中与设备位置双向同步、错误可上报、断开自动暂停。
- **桌面端画中画**：改用缩小窗口置顶的小窗，可拖动，关闭后恢复原窗口。
- **弹幕发送上传**：登录弹弹play 后可发送弹幕（未登录就地登录，成功自动续发）。
- **缓存策略降级**：蜂窝网络 / 低内存设备自动降档缓存，切换即时生效。
- **错误分级重试**：分级超时 + 初始 / 切集自动重开自愈，减少卡死。
- **声明式播放器菜单**、**字幕按视频记忆**（外部字幕 / 样式按 URL 记忆）、**防重叠轨道算法**、**seek 后弹幕位置稳定**。
- 播放器抽象层与异步会话取消统一，播放页拆分为多个模块文件，提升稳定性与可维护性。

### ⚙️ 设置 / 引导 / 更新
- **设置体系整理**：设置页结构重新归类，新增独立的「更新设置」页（更新检查 / 下载 / 提醒等）。
- **设置搜索补全**：内联设置弹窗的搜索也能命中全部具体设置项。
- **内容与源改名规则**：可自定义内容名 / 源名的改名配置与网络相关设置。
- **新手引导改版**：引导页内容与流程更新；操作时增加轻震动反馈。

### 🔗 跨类型 / 书架 / 图库
- **跨作品待读队列**（小说 / 漫画）：阅读器三点菜单「加入 / 打开队列」，书架收藏长按也可加入，打开自动移出。
- **统一图片收藏图库**：播放器截图 / 小说插图 / 漫画分镜统一入库，缩略图带来源角标。
- **书架排序 / 分类筛选**：按最新章时间 / 拼音序排序，分类按实际格式派生并隐藏无文件项，支持手动拖拽。
- **图库**：文件夹 + 多选、预下载接入正式下载、筛选收起、手机适配。

### 🔐 源登录 / 验证
- **源登录 API Key 模式**：登录面板按源配置自动显隐账号 / 密码 / API Key。
- **内嵌 WebView 登录 + 手动获取 Cookie 回灌**：登录改内嵌浏览器，浮动按钮手动回灌 Cookie。
- **Cloudflare 验证修复**：验证域改用移动 / 完整浏览器 UA，修复 turnstile 600010。
- **弹弹play 新用户注册**：未注册用户可在应用内直接注册弹弹play 账号，与账号页整合。

### 🐛 其他修复与加固
- 封面偶尔丢失修复；条漫上翻跨章无缝衔接（不再瞬移 / 闪帧）。
- 本地漫画阅读进度全面恢复：切话 / 回目录 / 点击退出 / 重进四类入口都能续读。
- 加密源视频下载小概率解析失败修复；单章 / 单集下载进度显示修复；横屏上滑取消 seek 失效修复。
- 翻页模式正文溢出显示区、导致页底文字被裁的排版问题修复。
- 本地导入视频无封面修复；划线颜色自定义 / 工具栏溢出 / 点击退出等小说阅读器多项打磨。
- 分类首屏冷启动自愈并预载首个分类，避免弹错；历史记录「隐藏」软删除保留进度。
- 构建侧同步 CI 的 Flutter 版本并钉定 PDF 依赖版本。
- 大量播放器 / 漫画 / 小说阅读器交互与稳定性修复。

> 无破坏性数据变更：v1.2.0 / beta.1 的收藏 / 进度 / 书签 / 历史 / 分组 / 评分 / 短评可直接沿用，旧源 JSON 无需修改、无需重新导入。沿用同一签名密钥，可直接覆盖安装。

---

## 🔞 年龄限制与免责说明

NexHub 默认开启**年龄限制保护**：标记为 **18+（成人 / mature）** 的源在默认设置下**自动隐藏**，不会出现在浏览、搜索与源列表中。如需访问，须在「设置 → 内容分级」中手动开启「显示限制级内容」，并确认你已年满法定成年年龄。

- **源作者责任**：请在源 JSON 中如实填写 `ageRating` 字段（`general` 全年龄 / `teen` 青少年 16+ / `mature` 成人 18+，支持 `all`/`16`/`r18`/`nsfw` 等别名，缺省 `general`）。应用仅依据该字段自动分级与隐藏，**最终的内容合规性与年龄适宜性由源的提供方与使用方负责**。
- **内容责任**：NexHub 为开源技术演示项目，本身**不提供、不存储、不中转任何内容**，所有内容均来自用户自行导入的源。对于源所提供内容的版权、合法性及适宜性，NexHub 不做任何形式的担保或背书。
- **合规提示**：若你所在地区法律禁止访问此类内容，或你未满法定成年年龄，请勿开启限制级内容显示。使用本软件即表示你已阅读、理解并同意仓库内完整的《免责声明》（依据中华人民共和国相关法律法规拟定）。

> 源字段完整说明见网站文档「源编写教程 · 源字段完整参考」一节，或仓库 `lib/core/models/plugin_config.dart`。

---

## 🛠️ 构建与发布

- CI 四平台（Android / Windows / Linux / macOS）build 命令均注入 `--dart-define=BANGUMI_CLIENT_ID` 与 `BANGUMI_CLIENT_SECRET`（来自仓库 Secrets），官方 Release 包的 Bangumi OAuth 登录开箱可用。
- Gradle 仓库顺序沿用既有修复（`google()` / `mavenCentral()` 优先、阿里云兜底），四平台均可正常出包。

---

## 📦 下载说明

- **Android**：APK 按 CPU 架构分包，绝大多数手机请选 **`arm64-v8a`**；很旧的机型选 `armeabi-v7a`；模拟器 / x86 平板选 `x86_64`。装错架构会提示「安装包无效」。也提供 **`app-release.apk`** 通用包（单文件全架构，无需挑架构，体积较大）。
- **Windows**：`NexHub-setup-*.exe` 为安装包（支持中英文、非管理员安装），`NexHub-windows-x64.zip` 为免安装便携版。若被 SmartScreen 拦截，点「更多信息 → 仍要运行」。
- **Linux**：`NexHub-linux-x64.tar.gz` 为免安装版（解压即运行）；`NexHub-linux-x64.deb` 为 Debian / Ubuntu 系安装包；`NexHub-linux-x64.AppImage` 为单文件便携版（赋予可执行权限后双击运行）。
- **macOS**：`NexHub-macos.zip` 为免安装版，`NexHub-macos.dmg` 为磁盘镜像（挂载后拖入「应用程序」）。首次打开需在「系统设置 → 隐私与安全性」中放行。

---

## ⚠️ 已知限制

1. **ECH 暂不生效**：网络设置中的 ECH 界面已就绪，运行时未接通；**SNI 仅尽力生效**（受 Dart TLS 栈限制）；
2. **自建构建的 Bangumi OAuth 不可用**：未注入凭据时请改用 Access Token 登录，或自行到 [bgm.tv/dev/app/create](https://bgm.tv/dev/app/create) 注册应用（回调地址 `nexhub://oauth/callback`）；
3. **Bangumi 吐槽仅只读**；源站评论的可用操作取决于源 JSON 声明了哪些路由；
4. 遇到问题优先到 Issues 反馈。

---

## 🙏 参与共创

- 反馈 Bug → [Issues](https://github.com/nexhub-app/Nexhub/issues)
- 路线规划 / 新功能想法 → [Discussions](https://github.com/nexhub-app/Nexhub/discussions)
- 写源 / 改引擎 → 欢迎 Pull Request

> NexHub 不内置任何内容源。首次安装后需要自行导入源，才能看到内容。
