# NexHub v0.3.1

> 四合一媒体聚合客户端（动漫 / 漫画 / 小说 / 影视）—— 源即插件 · 共创社区。

> ✅ **升级提示（Android）**：本版**沿用 v0.3.0 同一 APK 签名密钥**（仅存放方式升级为经 CI 安全注入、不再入库），从 v0.3.0 **可直接覆盖安装**（无需卸载）。仅当你仍停留在更早的随机签名旧包时，才需先卸载一次再装。

## ✨ 本次更新（v0.3.1）

本版聚焦**视频播放内核与解码健壮性**，补充**中国传统色主题**与**签名管理生产级升级**；同时提升 Android 在部分机型与国内网络下的构建 / 渲染稳定性，并修复若干解析与弹幕匹配问题。

### 🎞️ 视频后端简化
- 移除 `fvp` 视频后端依赖，统一为 **media_kit + media_kit_libs_video（libmpv）** 单一内核路径；清理 fvp 注册与注释。

### 📊 新增：播放统计面板
- 新增 `PlayerStats` 模型，读取 mpv 只读属性（实际解码器 `hwdec-current`、编码、像素格式、分辨率、渲染 / 解码掉帧、码率、缓冲进度）。
- 播放器「更多」菜单新增「播放统计」项，展示当前解码方式（软 / 硬解）、编码、像素格式、分辨率、掉帧、码率与缓存缓冲。

### 🛡️ 解码异常自动降级与自愈
- 硬件解码失败（花屏）时自动降级为软件解码并提示；播放页 re-open 当前地址使新解码路径真正生效。
- 手动切换解码模式同样 re-open 生效；新增「硬件解码+」选项提示「花屏时推荐」。

### 🔐 签名管理生产级升级（安全性）
- 固定签名密钥不再提交入库：`android/app/upload-keystore.jks` 改由 **CI Secret 注入**，密码存放于 `android/key.properties`（不入库，提供 `key.properties.example` 模板）；`.gitignore` 补充密钥临时文件忽略。
- 签名密钥本身与 v0.3.0 完全一致，**从 v0.3.0 仍可覆盖安装**（无需卸载）。

### 💬 弹幕匹配修复
- 修复跨标题误匹配：弹幕库匹配须 `isMatched` 或标题相似，取消盲选回退，避免错配到不相关的弹幕库。

### 🔎 解析 / 嗅探修复
- 笔趣阁类源验证拦截修复 + Cookie 持久化，提升小说源稳定性。
- 视频嗅探稳定性修复（含此前对象库损坏后重建丢失提交的修复）。

### 🤖 Android 渲染修复（花屏）
- `AndroidManifest.xml` 增加 `io.flutter.embedding.android.EnableImpeller=false`，回退 Skia 渲染，修复部分旧 GPU / 驱动（Mali、旧 Adreno）在 Impeller 下视频外纹理**闪烁 / 花屏**。

### 🔧 Android 构建可靠性（国内网络）
- `build.gradle.kts` / `settings.gradle.kts` 增加阿里云 maven 镜像（google / central）优先、官方源兜底。
- `gradle.properties` 增加 Gradle HTTP 超时（60s）与最大重试（3 次），避免下载超时无限假死。

### 🎨 主题：6 套中国传统色预设
- 新增 **茶红 / 桃夭 / 天水碧 / 朱殷 / 群青 / 玄色** 种子色（含中文名）。
- 修复**玄色渲染**：选中玄色走专属「墨黑 + 赤」主题；预设色块长按显示中文色名。

### 🖥️ 界面
- 详情页窄屏响应式布局优化；在线浏览页「布局」按钮重复显示修复，统一布局入口。
- 视频嗅探页提示文字被挤成竖排修复。

> 完整历史更新记录见仓库 `RELEASE_NOTES.md`。

## 🔌 重要：先导入源

本应用**不内置任何源**，首次打开是空的，请先导入至少一个源 JSON（应用内「源管理 / 导入源」）才能看到内容。源由社区共享维护，请通过社区渠道获取，并自行评估其合规性与内容合法性。

## ⚠️ 免责声明

NexHub 是一款开源技术演示项目，本身**不提供、不存储、不中转任何受版权保护的内容**，也**不内置任何内容源**。所有内容均来自用户自行导入的源，其合法性与版权状况由源的提供方与使用方负责。使用本软件即表示你已阅读、理解并同意仓库内完整的《免责声明》。
