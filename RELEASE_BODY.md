# NexHub v0.4.5

> 四合一媒体聚合客户端（动漫 / 漫画 / 小说 / 影视）—— 源即插件 · 共创社区。

---

## ⚠️ 请先读这一段

本版是 **v0.4.4 的跟进版**，聚焦两大块体验修复：**影视播放器退出横屏恢复**，以及**条漫（Webtoon）阅读器的割裂感、细白条与选集界面窄屏显示问题**。

- **依然是 0.4.x 预览版**：相对稳定的 v0.3.1 仍是「求稳用户」的推荐版本；愿意试用新功能请先**备份数据**（设置 → 备份 / 导出，以及导出你的源 JSON）。
- **这是一次小版本跟进**：仅含 2 个 bugfix 提交，未引入新功能，可直接在 v0.4.4 基础上覆盖升级。
- 遇到问题请到 [Issues](https://github.com/nexhub-app/Nexhub/issues) 反馈，**附上复现步骤 + 所用模块（动漫/漫画/小说/影视）+ 源类型**。

### 数据兼容性（好消息）

经代码核查，**本版没有破坏性数据变更**：v0.4.4 的收藏、进度、书签、历史、分组、评分、短评**可直接沿用**；**旧的源 JSON 无需修改、无需重新导入**。

### 升级方式（Android）

本版**沿用与 v0.3.0 / v0.3.1 / v0.4.0 / v0.4.1 / v0.4.2 / v0.4.3 / v0.4.4 相同的签名密钥**，可以**直接覆盖安装**，无需卸载。

---

## 🔭 路线规划（规划中 · 欢迎参与）

以下功能**尚未实现**，是接下来几个版本的重点方向。它们的设计同样存在大量取舍，欢迎在 [Discussions](https://github.com/nexhub-app/NexHub/discussions) 或 Issues 提意见：

1. **接入 AI 能力**：在应用内引入 AI 辅助，例如智能搜源、内容摘要、看番/看书助手、自然语言检索等。
2. **小说翻译**：为小说阅读器接入机翻管线，支持原文 / 译文对照、按需段落翻译与缓存。
3. **漫画翻译（MTL）**：探索漫画图片的机翻嵌字 / 气泡替换，降低跨语言漫画的阅读门槛。
4. **视频实时翻译**：影视模块的字幕 / 实时字幕翻译，支持外挂与内嵌字幕的语言切换。
5. **增加其他同步方式**：在现有 Bangumi 同步之外，接入更多同步后端（如 AniList、MyAnimeList、Trakt、SIMKL、MDList 等），并支持跨后端双向同步与冲突策略可配置。

> 以上均为规划项，不含在本次发布中；具体落地顺序与形态会以社区反馈调整。

---

## ✨ 本次更新内容（v0.4.4 → v0.4.5）

> 相对 v0.4.4 共 2 个提交，聚焦影视播放器与条漫阅读器的体验修复。

### 🎬 影视播放器修复

- **视频退出横屏恢复**：修复从全屏播放退出后屏幕方向残留横屏、未恢复进入前方向的问题。原逻辑仅在「经过全屏」时才还原方向，而进入播放页默认锁定横屏（`lockOrientation: landscape`），导致退出后方向被锁死。现改为退出播放页时**无条件还原方向**（`SystemChrome.setPreferredOrientations([])`），桌面 / 平板不受影响。

### 🖼️ 条漫（Webtoon）阅读器修复

- **消除割裂感**：图片加载完成后移除占位约束、按真实图高显示，根治长条图拼接处的「割裂 / 跳变」观感（参考 Mihon `WebtoonPageHolder` 与 VeneraX `ComicImage` 的「加载完成即移占位、0 空白带」做法；`SourceImage` 新增 `onLoadComplete` 回调支撑占位移除）。
- **消除细白条**：修复图片之间的缝隙透出浅色背景、形成「细白条」的问题，长条图连续滚动不再露白。
- **选集界面手机端显示**：选集 `BottomSheet` 限高由 0.6 屏高放宽到 0.85 屏高，集数列表改为紧凑布局，线路改为横向 Chip 行——窄屏（手机端）下选集与线路正常展示，不再拥挤或显示不全。

> 修复范围聚焦影视播放器与条漫（Webtoon 连续滚动）阅读器，其余功能与 v0.4.4 完全一致；无数据格式变更，旧数据与源 JSON 可直接沿用。

---

## 🔞 年龄限制与免责说明

NexHub 默认开启**年龄限制保护**：标记为 **18+（成人 / mature）** 的源在默认设置下**自动隐藏**，不会出现在浏览、搜索与源列表中。如需访问，须在「设置 → 内容分级」中手动开启「显示限制级内容」，并确认你已年满法定成年年龄。

- **源作者责任**：请在源 JSON 中如实填写 `ageRating` 字段（`general` 全年龄 / `teen` 青少年 16+ / `mature` 成人 18+，支持 `all`/`16`/`r18`/`nsfw` 等别名，缺省 `general`）。应用仅依据该字段自动分级与隐藏，**最终的内容合规性与年龄适宜性由源的提供方与使用方负责**。
- **内容责任**：NexHub 为开源技术演示项目，本身**不提供、不存储、不中转任何内容**，所有内容均来自用户自行导入的源。对于源所提供内容的版权、合法性及适宜性，NexHub 不做任何形式的担保或背书。
- **合规提示**：若你所在地区法律禁止访问此类内容，或你未满法定成年年龄，请勿开启限制级内容显示。使用本软件即表示你已阅读、理解并同意仓库内完整的《免责声明》（依据中华人民共和国相关法律法规拟定）。

> 源字段完整说明见网站文档「源编写教程 · 源字段完整参考」一节，或仓库 `lib/core/models/plugin_config.dart`。

---

## 🛠️ 构建与发布

- CI 四平台（Android / Windows / Linux / macOS）build 命令均注入 `--dart-define=BANGUMI_CLIENT_ID` 与 `BANGUMI_CLIENT_SECRET`（来自仓库 Secrets），官方 Release 包的 Bangumi OAuth 登录开箱可用。
- Gradle 仓库顺序沿用 v0.4.1 的修复（`google()` / `mavenCentral()` 优先、阿里云兜底），四平台均可正常出包。

---

## 📦 下载说明

- **Android**：APK 按 CPU 架构分包，绝大多数手机请选 **`arm64-v8a`**；很旧的机型选 `armeabi-v7a`；模拟器 / x86 平板选 `x86_64`。装错架构会提示「安装包无效」。
- **Windows**：`NexHub-setup-*.exe` 为安装包（支持中英文、非管理员安装），`NexHub-windows-x64.zip` 为免安装便携版。若被 SmartScreen 拦截，点「更多信息 → 仍要运行」。
- **Linux / macOS**：分别为 `.tar.gz` 与 `.zip`。macOS 首次打开需在「系统设置 → 隐私与安全性」中放行。

---

## ⚠️ 已知限制

1. **0.4.x 仍为预览版**，部分高级功能仍在打磨，遇到问题优先备份数据并到 Issues 反馈；
2. **ECH 暂不生效**：网络设置中的 ECH 界面已就绪，运行时未接通；**SNI 仅尽力生效**（受 Dart TLS 栈限制）；
3. **自建构建的 Bangumi OAuth 不可用**：未注入凭据时请改用 Access Token 登录，或自行到 [bgm.tv/dev/app/create](https://bgm.tv/dev/app/create) 注册应用（回调地址 `nexhub://oauth/callback`）；
4. **Bangumi 吐槽仅只读**；源站评论的可用操作取决于源 JSON 声明了哪些路由。

---

## 🙏 参与共创

- 反馈 Bug → [Issues](https://github.com/nexhub-app/NexHub/issues)
- 路线规划 / 新功能想法 → [Discussions](https://github.com/nexhub-app/NexHub/discussions)
- 写源 / 改引擎 → 欢迎 Pull Request

> NexHub 不内置任何内容源。首次安装后需要自行导入源，才能看到内容。
