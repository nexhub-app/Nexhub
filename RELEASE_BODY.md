# NexHub v2.0.0-beta.6（预发布测试版）

> 四合一媒体聚合客户端（动漫 / 漫画 / 小说 / 影视）—— 源即插件 · 共创社区。

---

## ⚠️ 本版说明（预发布 · beta 测试）

这是 **2.0.0 的预发布测试版（beta.6）**，不是正式稳定版。在 beta.5 的基础上，本次是一次**以 RSS 订阅阅读为主的大型更新**——RSS 从「浏览页里一个简单的列表」升级为**独立的完整阅读模块**：离线缓存、全文抓取、图片画廊、文章内视频与播客播放、分组与 OPML 导入导出、站内搜索、系统通知，以及一批实机反馈的媒体加载与排版修复。它**仍在测试中**，可能有个别未发现的问题。

### 相对 v2.0.0-beta.5 的主要变化（亮点）

- **RSS 变成真正的阅读器**：订阅列表、分组管理、离线缓存（含正文，断网可读）、已读置灰、跨源收藏页、单源与全局搜索，一步到位。
- **图文与媒体体验**：文章内图片可点开进画廊缩放 / 保存 / 分享；视频直接内嵌播放（手势、锁屏、比例、进度记忆与主播放器一致）；带音频附件的播客源可直接播放。
- **订阅导入更省事**：支持 OPML 文件 / 粘贴文本导入与导出，也支持输入网址自动发现订阅源。
- **更省流量、更稳**：条件 GET（304 复用本地缓存）、抓取走统一网络配置、非 UTF-8 编码不再乱码、DNS 多 IP 逐个重试。

> 底层仍为 Flutter 3.47，最低 Android 仍为 7.0。

### 已知风险（请知悉）

- beta 版可能仍有个别 bug；本次改动集中在 RSS 与文章阅读链路，其他模块未做改动。
- 全文抓取、OPML 网站发现、播客播放依赖真实网络与源站点结构，个别站点表现可能有差异，欢迎反馈。
- 追求绝对稳定的用户可继续用 v2.0.0-beta.5，等正式版；想抢先体验的欢迎升级并反馈问题。

### 数据兼容性（好消息）

经代码核查，**本版没有破坏性数据变更**：beta.5 的收藏、进度、书签、历史、分组、评分、短评**可直接沿用**；**旧的源 JSON 无需修改、无需重新导入**。RSS 订阅与新增的缓存 / 已读 / 收藏状态采用新的本地存储键，首次进入时会自行建立，不影响既有数据。

### 升级方式（Android）

本版**沿用与历史版本相同的签名密钥**，可以**直接覆盖安装**，无需卸载。

> 已在使用 beta.5 的用户：本次未改动动漫 / 漫画 / 影视内容链路，升级后原有使用方式不变，只新增了 RSS 阅读能力。

---

## ✨ 本次更新内容（v2.0.0-beta.5 → v2.0.0-beta.6）

> 相对 beta.5 共 8 个提交。以下为面向用户的重点，完整逐条记录见 [RELEASE_NOTES](./RELEASE_NOTES.md)。

### 📡 RSS 订阅管理

- **绑定分类 / 移回全局**：订阅可在「全局订阅」与小说 / 漫画 / 影视分类之间自由绑定和解绑，全局列表始终显示全部订阅，分类页只显示本分类的。
- **分组（多标签）**：一个订阅可同时属于多个分组。订阅列表顶部有水平滚动的分组筛选条，可新建、重命名、合并与删除分组（删除分组不会删除订阅）。
- **OPML 导入 / 导出**：支持选择 `.opml` / `.xml` 文件导入，也支持直接粘贴 OPML 文本；导入前可逐条勾选预览。导出会按分组整理后通过系统分享发出。
- **网站自动发现**：输入站点地址，自动从页面里找出可用的订阅源并列出候选，勾选即可添加。
- **收藏夹图标**：订阅列表现在显示站点 favicon（取不到时回退为通用 RSS 图标）。

### 📖 RSS 阅读体验

- **离线缓存与全文抓取**：打开文章时自动抓取并缓存正文，之后断网也能读；抓取失败会回退到已有的离线内容并给出提示。
- **已读与收藏**：打开即标记已读（已读条目整条置灰），单篇可星标收藏，另有跨源的「RSS 收藏」汇总页，入口在「设置 → 数据与账户」。
- **站内搜索**：支持在单个订阅内搜索，也支持跨全部订阅的全局搜索（基于本地缓存，离线可用），带命中计数。
- **图片画廊**：文章内图片点击进全屏画廊，支持双指缩放、拖动、保存与分享。
- **阅读设置分门归类**：字号、行距、对齐、页边距等按类别分组呈现，并新增夜间模式（跟随应用主题），长文阅读更舒服。
- **内容排版与消毒**：列表与正文的排版对齐统一；正文渲染会跳过脚本、内嵌框架、表单等元素，点击外链走系统浏览器，并拦截不安全的链接协议。

### 🎬 文章内媒体播放

- **视频内嵌播放**：文章里的视频可直接在页面内播放，无需跳转；控制条、进度、时长与底部定位均已修复，并同步了主播放器的缓冲指示。
- **播放器能力对齐**：内嵌播放器支持手势调节（亮度 / 音量 / 进度）、锁定控制条、画面比例切换与播放进度记忆，操作习惯和主播放器一致。
- **播客与附件**：带音频附件的订阅可在文章页直接播放（支持多音频切换、拖动进度）；视频 / 文件类附件提供「打开附件」走系统处理。
- **图片加载修复**：修复部分站点图片因缺少来源标识而返回 403 无法加载的问题；修复音频因类型识别失败而显示 0:00 的问题。

### 🔔 更新提醒

- **系统通知**：新增「系统通知」开关，检测到订阅更新时发一条系统级通知汇总。Windows 与 Web 平台不支持系统通知，会自动降级为应用内未读标记，不影响使用。
- **充电时自动检测**：RSS 更新检测新增「充电时自动检测」触发条件，与原有的「打开应用时」并存。

### 🧩 格式兼容与解析

- **四种格式全支持**：RSS 2.0、Atom、RDF（RSS 1.0）、JSON Feed 都能正常解析，不再出现「不支持的格式」报错。
- **编码不再乱码**：针对声明为非 UTF-8 的订阅（中文 GBK 系列、西欧 Latin-1 系列）按声明正确解码，乱码问题已修复。
- **相对链接自动补全**：条目链接、封面图、附件地址以及订阅的站点地址与图标地址，都会按订阅地址补全为绝对地址，避免点开是死链。
- **条件 GET 省流量**：支持 ETag / Last-Modified，服务端返回 304 时直接复用本地缓存，刷新不再重复下载整份内容。

### 🌐 网络与连通性

- **DNS 多地址逐个重试**：自定义 DNS / 钉 IP 场景下，解析结果含多个 IP 时不再只试第一个——第一个不可达会继续尝试后续地址，修复「浏览器能打开、应用却连接超时」的问题。

### 🐛 其他修复与加固

- 修复抓取超时导致订阅刷新失败的问题；列表改为「先展示缓存、后台再刷新」，打开更快。
- 修复内嵌视频的时序竞态（偶发提示连接失败、无法播放）。
- 全量补齐中英文案（新增 150+ 条本地化文案，覆盖长文阅读、附件、分组、OPML、搜索、通知等相关界面）。

> 无破坏性数据变更：beta.5 的收藏 / 进度 / 书签 / 历史 / 分组 / 评分 / 短评可直接沿用，旧源 JSON 无需修改、无需重新导入。沿用同一签名密钥，可直接覆盖安装。

---

## 🔞 年龄限制与免责说明

NexHub 默认开启**年龄限制保护**：标记为 **18+（成人 / mature）** 的源在默认设置下**自动隐藏**，不会出现在浏览、搜索与源列表中。如需访问，须在「设置 → 内容分级」中手动开启「显示限制级内容」，并确认你已年满法定成年年龄。

- **源作者责任**：请在源 JSON 中如实填写 `ageRating` 字段（`general` 全年龄 / `teen` 青少年 16+ / `mature` 成人 18+，支持 `all`/`16`/`r18`/`nsfw` 等别名，缺省 `general`）。应用仅依据该字段自动分级与隐藏，**最终的内容合规性与年龄适宜性由源的提供方与使用方负责**。
- **内容责任**：NexHub 为开源技术演示项目，本身**不提供、不存储、不中转任何内容**，所有内容均来自用户自行导入的源。对于源所提供内容的版权、合法性及适宜性，NexHub 不做任何形式的担保或背书。
- **合规提示**：若你所在地区法律禁止访问此类内容，或你未满法定成年年龄，请勿开启限制级内容显示。使用本软件即表示你已阅读、理解并同意仓库内完整的《免责声明》（依据中华人民共和国相关法律法规拟定）。

> RSS 订阅的内容同样完全由用户自行添加，应用不对任何订阅源的内容负责。

> 源字段完整说明见网站文档「源编写教程 · 源字段完整参考」一节，或仓库 `lib/core/models/plugin_config.dart`。

---

## 🛠️ 构建与发布

- CI 四平台（Android / Windows / Linux / macOS）build 命令均注入 `--dart-define=BANGUMI_CLIENT_ID` 与 `BANGUMI_CLIENT_SECRET`（来自仓库 Secrets），官方 Release 包的 Bangumi OAuth 登录开箱可用。
- Gradle 仓库顺序沿用既有修复（`google()` / `mavenCentral()` 优先、阿里云兜底），四平台均可正常出包。
- 本版新增两个依赖：`flutter_local_notifications`（系统通知，仅 Android / iOS / Linux / macOS 生效）与 `http`（让图片下载复用应用统一网络层），均已纳入版本锁定。

---

## 📦 下载说明

- **Android**：APK 按 CPU 架构分包，绝大多数手机请选 **`arm64-v8a`**；很旧的机型选 `armeabi-v7a`；模拟器 / x86 平板选 `x86_64`。装错架构会提示「安装包无效」。也提供 **`app-release.apk`** 通用包（单文件全架构，无需挑架构，体积较大）。
- **Windows**：`NexHub-setup-*.exe` 为安装包（支持中英文、非管理员安装），`NexHub-windows-x64.zip` 为免安装便携版。若被 SmartScreen 拦截，点「更多信息 → 仍要运行」。
- **Linux**：`NexHub-linux-x64.tar.gz` 为免安装版（解压即运行）；`NexHub-linux-x64.deb` 为 Debian / Ubuntu 系安装包；`NexHub-linux-x64.AppImage` 为单文件便携版（赋予可执行权限后双击运行）。
- **macOS**：`NexHub-macos.zip` 为免安装版，`NexHub-macos.dmg` 为磁盘镜像（挂载后拖入「应用程序」）。首次打开需在「系统设置 → 隐私与安全性」中放行。

---

## ⚠️ 已知限制

1. **系统通知不支持 Windows / Web**：受平台能力限制，这两个平台只保留应用内未读标记。
2. **ECH 暂不生效**：Dart TLS 栈无 ECH API、无可用插件，直连暂不可行；替代路径：代理模式选手动指向支持 ECH 的本地内核，或开启系统安全 DNS 由 WebView 路径自动启用 ECH。
3. **SNI 运行时可控但仍受栈限制**：现可在网络设置中配置「域名 → SNI 映射」与「免 SNI（绕 SNI 封锁）」，仅对直连 https 生效；走外部代理时由代理自理；部分站点仍需实测。
4. **自建构建的 Bangumi OAuth 不可用**：未注入凭据时请改用 Access Token 登录，或自行到 [bgm.tv/dev/app/create](https://bgm.tv/dev/app/create) 注册应用（回调地址 `nexhub://oauth/callback`）；
5. **Bangumi 吐槽仅只读**；源站评论的可用操作取决于源 JSON 声明了哪些路由；
6. **RSS 全文抓取效果因站而异**：正文抽取会过滤导航、页脚等区域，个别站点排版可能不完整；远程图片仍会加载。
7. 遇到问题优先到 Issues 反馈。

---

## 🙏 参与共创

- 反馈 Bug → [Issues](https://github.com/nexhub-app/Nexhub/issues)
- 路线规划 / 新功能想法 → [Discussions](https://github.com/nexhub-app/Nexhub/discussions)
- 写源 / 改引擎 → 欢迎 Pull Request

> NexHub 不内置任何内容源。首次安装后需要自行导入源，才能看到内容。
