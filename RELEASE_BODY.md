# NexHub v2.0.0-beta.4（预发布测试版）

> 四合一媒体聚合客户端（动漫 / 漫画 / 小说 / 影视）—— 源即插件 · 共创社区。

---

## ⚠️ 本版说明（预发布 · beta 测试）

这是 **2.0.0 的预发布测试版（beta.4）**，不是正式稳定版。在 beta.3 的基础上，本次重点打磨**跨语言翻译的可靠性与可用性**——翻译接口主备容灾、章节翻译可断点续译、术语表注入、可选润色与证据审校、一键导出可复用译文；并对**网络连通性、收藏、下载、更新通道、源库**做了大量加固。它**仍在测试中**，可能有个别未发现的问题。

### 相对 v2.0.0-beta.3 的主要变化（亮点）

- **翻译可靠性全套升级**：主备端点 + 健康熔断，AI 接口偶发抖动不再整段失败；章节翻译支持断点续译；术语表注入让专有名词翻译一致；可选润色与证据审校；进翻译前先生成全书总览。
- **漫画页翻译气泡回填**：译文气泡按版面回填，翻页残留更少、覆盖更准。
- **视频整片离线字幕翻译 + 双语导出**：整片字幕离线翻完，可导出双语字幕文件。
- **网络层防限流**：同站请求串行化 + 按错误性质智能退避，e-hentai 等站点不再瞬时空白；修复流式下载连接泄漏。
- **网络收藏修复**：解析空列表、应用内文件夹切换 + 选夹添加、错误可重试。
- **收藏修复**：详情页可收藏、图片收藏持久化、收藏弹窗新增「收藏本章」。
- **下载 / 更新加固**：下载遗留产物按条目隔离 + meta 回退；更新候选链降级重试 + 测速严格 + 自定义镜像稳健。
- **源库**：年龄限制下自动隐藏 18+ 源 + 全应用触感反馈；构建链升级 Gradle 9.1 / AGP 9.0.1 / Kotlin 2.3.20。

> 底层仍为 Flutter 3.47，最低 Android 仍为 7.0。

### 已知风险（请知悉）

- beta 版可能仍有个别 bug；漫画页翻译 / 视频字幕翻译依赖 AI 接口配置，未配置时回落通用翻译或不可用，升级如遇问题请反馈。
- 追求绝对稳定的用户可继续用 v2.0.0-beta.3，等正式版；想抢先体验的欢迎升级并反馈问题。

### 数据兼容性（好消息）

经代码核查，**本版没有破坏性数据变更**：beta.3 的收藏、进度、书签、历史、分组、评分、短评**可直接沿用**；**旧的源 JSON 无需修改、无需重新导入**。

### 升级方式（Android）

本版**沿用与历史版本相同的签名密钥**，可以**直接覆盖安装**，无需卸载。

> 已在使用 beta.3 的用户：本次进一步加固测试版通道（候选链降级重试、测速严格校验、自定义镜像健壮性），升级后应用内更新检查将更稳地提示后续预发布。

---

## ✨ 本次更新内容（v2.0.0-beta.3 → v2.0.0-beta.4）

> 相对 beta.3 共 20 个提交。以下为面向用户的重点，完整逐条记录见 [RELEASE_NOTES](./RELEASE_NOTES.md)。

### 🌐 翻译（跨语言阅读）

- **翻译接口主备容灾**：主端点失败自动切备用端点，带健康熔断（连续失败临时熔断、恢复探测），AI 接口偶发 5xx / 超时不再让整段翻译失败。
- **章节翻译断点续译**：按块写入检查点，中断后可从断点继续，不必重翻整章。
- **术语表注入 + 统一提示词管线**：专有名词跨段一致，多书可共享术语表；翻译提示词统一收口，结果更稳。
- **可选润色 + 证据驱动审校**：译文可选二次润色，关键句附原文证据便于核对。
- **书籍预扫描**：进翻译前先生成章节摘要 + 全书总览注入，长书上下文更连贯。
- **可复用译文资源导出**：把译文导出为可复用资产，便于二次利用。
- **漫画页翻译气泡回填**：译文气泡按版面回填渲染，翻页后残留更少、覆盖更准。
- **视频整片离线字幕翻译 + 双语导出**：整片字幕离线翻完，可导出双语字幕文件。

### 🔄 更新通道

- 下载候选链降级重试：某一镜像失败时自动降级到下一候选；测速改为严格校验，避免错选慢镜像；自定义镜像地址健壮性增强（上次 beta.3 修复了测试版通道「收不到新版本」，本次修复候选链与测速）。

### 🛰️ 网络与连通性

- **同站请求串行化（防突发）+ 智能退避**：同站请求严格一前一后放行，杜绝扎堆触发限流；重试按错误性质区分——连接重置（RST）补重试、服务端挂起（限流）单补 + 长退避，避免给已限流站点火上浇油；修复流式下载重定向连接泄漏。
- **网络收藏修复**：修复收藏列表走错解析入口导致的空白（源声明 `listEntry` 走正确入口）；新增应用内文件夹切换 + 选夹添加；错误不再静默吞掉，改为错误态 + 重试入口。
- **e-hentai 登录粘贴 Cookie 归一化**：粘贴任意常见格式（含换行 / 冒号分隔）都能正确登录并解析，告别「提示成功却显示未登录」。

### 📚 收藏 / 下载

- **收藏修复**：修复详情页无法收藏、图片收藏持久化丢失；收藏弹窗新增「收藏本章」。
- **下载修复**：遗留产物按条目隔离，meta 写入失败回退下载根目录，减少整包损坏。

### 🧩 源库

- **年龄限制下源库自动隐藏 18+ 源**：标记为 mature 的源在默认设置下自动隐藏，源管理页移除隐藏提示；全应用补齐触感反馈（振动），操作手感更跟手。

### 🐛 修复与加固

- 修复对话框 `TextEditingController` 在退场动画期被释放导致的连锁崩溃。
- 视频后台播放通知初始化失败修复，移除 `demo_multi_line` 死引用。
- 构建链升级 Gradle 9.1.0 / AGP 9.0.1 / Kotlin 2.3.20，构建更稳。
- `gen_icons.py` 输出路径统一走 `out_path` 校验。
- 文档：新增「翻译进阶」章节与引导入口。

> 无破坏性数据变更：beta.3 的收藏 / 进度 / 书签 / 历史 / 分组 / 评分 / 短评可直接沿用，旧源 JSON 无需修改、无需重新导入。沿用同一签名密钥，可直接覆盖安装。

---

## 🔞 年龄限制与免责说明

NexHub 默认开启**年龄限制保护**：标记为 **18+（成人 / mature）** 的源在默认设置下**自动隐藏**，不会出现在浏览、搜索与源列表中。如需访问，须在「设置 → 内容分级」中手动开启「显示限制级内容」，并确认你已年满法定成年年龄。

- **源作者责任**：请在源 JSON 中如实填写 `ageRating` 字段（`general` 全年龄 / `teen` 青少年 16+ / `mature` 成人 18+，支持 `all`/`16`/`r18`/`nsfw` 等别名，缺省 `general`）。应用仅依据该字段自动分级与隐藏，**最终的内容合规性与年龄适宜性由源的提供方与使用方负责**。
- **内容责任**：NexHub 为开源技术演示项目，本身**不提供、不存储、不中转任何内容**，所有内容均来自用户自行导入的源。对于源所提供内容的版权、合法性及适宜性，NexHub 不做任何形式的担保或背书。
- **合规提示**：若你所在地区法律禁止访问此类内容，或你未满法定成年年龄，请勿开启限制级内容显示。使用本软件即表示你已阅读、理解并同意仓库内完整的《免责声明》（依据中华人民共和国相关法律法规拟定）。

> 源字段完整说明见网站文档「源编写教程 · 源字段完整参考」一节，或仓库 `lib/core/models/plugin_config.dart`。

---

## 🛠️ 构建与发布

- CI 四平台（Android / Windows / Linux / macOS）build 命令均注入 `--dart-define=BANGUMI_CLIENT_ID` 与 `BANGUMI_CLIENT_SECRET`（来自仓库 Secrets），官方 Release 包的 Bangumi OAuth 登录开箱可用。
- Gradle 仓库顺序沿用既有修复（`google()` / `mavenCentral()` 优先、阿里云兜底），四平台均可正常出包。

---

## 📦 下载说明

- **Android**：APK 按 CPU 架构分包，绝大多数手机请选 **`arm64-v8a`**；很旧的机型选 `armeabi-v7a`；模拟器 / x86 平板选 `x86_64`。装错架构会提示「安装包无效」。也提供 **`app-release.apk`** 通用包（单文件全架构，无需挑架构，体积较大）。
- **Windows**：`NexHub-setup-*.exe` 为安装包（支持中英文、非管理员安装），`NexHub-windows-x64.zip` 为免安装便携版。若被 SmartScreen 拦截，点「更多信息 → 仍要运行」。
- **Linux**：`NexHub-linux-x64.tar.gz` 为免安装版（解压即运行）；`NexHub-linux-x64.deb` 为 Debian / Ubuntu 系安装包；`NexHub-linux-x64.AppImage` 为单文件便携版（赋予可执行权限后双击运行）。
- **macOS**：`NexHub-macos.zip` 为免安装版，`NexHub-macos.dmg` 为磁盘镜像（挂载后拖入「应用程序」）。首次打开需在「系统设置 → 隐私与安全性」中放行。

---

## ⚠️ 已知限制

1. **ECH 暂不生效**：Dart TLS 栈无 ECH API、无可用插件，直连暂不可行；替代路径：代理模式选手动指向支持 ECH 的本地内核，或开启系统安全 DNS 由 WebView 路径自动启用 ECH。
2. **SNI 运行时可控但仍受栈限制**：现可在网络设置中配置「域名 → SNI 映射」与「免 SNI（绕 SNI 封锁）」，仅对直连 https 生效；走外部代理时由代理自理；部分站点仍需实测。
3. **自建构建的 Bangumi OAuth 不可用**：未注入凭据时请改用 Access Token 登录，或自行到 [bgm.tv/dev/app/create](https://bgm.tv/dev/app/create) 注册应用（回调地址 `nexhub://oauth/callback`）；
4. **Bangumi 吐槽仅只读**；源站评论的可用操作取决于源 JSON 声明了哪些路由；
5. 遇到问题优先到 Issues 反馈。

---

## 🙏 参与共创

- 反馈 Bug → [Issues](https://github.com/nexhub-app/Nexhub/issues)
- 路线规划 / 新功能想法 → [Discussions](https://github.com/nexhub-app/Nexhub/discussions)
- 写源 / 改引擎 → 欢迎 Pull Request

> NexHub 不内置任何内容源。首次安装后需要自行导入源，才能看到内容。
