# NexHub v0.4.1

> 四合一媒体聚合客户端（动漫 / 漫画 / 小说 / 影视）—— 源即插件 · 共创社区。

---

## ⚠️ 请先读这一段

本版是 **v0.4.0 的跟进版本**，主要做三件事：**补齐 v0.4.0 因 CI 构建失败而缺漏的发布包**、**强化 Bangumi 同步与源管理**、以及一批阅读器 / 漫画 / 暗色模式的体验修复。

- **依然是 0.4.x 预览版**：相对稳定的 v0.3.1 仍是「求稳用户」的推荐版本；愿意试用新功能请先**备份数据**（设置 → 备份 / 导出，以及导出你的源 JSON）。
- **本次最重要的修复**：v0.4.0 在海外 CI 上因 Gradle 把阿里云镜像排在最前、拉不到 Google 构件而**构建失败、部分平台没能出包**；本版已把 `google()` / `mavenCentral()` 置为优先、阿里云降为兜底，四平台（Android / Windows / Linux / macOS）均可正常出包。
- 遇到问题请到 [Issues](https://github.com/nexhub-app/Nexhub/issues) 反馈，**附上复现步骤 + 所用模块（动漫/漫画/小说/影视）+ 源类型**。

### 数据兼容性（好消息）

经代码核查，**本版没有破坏性数据变更**：v0.4.0 的收藏、进度、书签、历史、分组、评分、短评**可直接沿用**；新增字段均为追加，旧数据读取不受影响；**旧的源 JSON 无需修改、无需重新导入**。

### 升级方式（Android）

本版**沿用与 v0.3.0 / v0.3.1 / v0.4.0 相同的签名密钥**，可以**直接覆盖安装**，无需卸载。

---

## 🔭 路线规划（规划中 · 欢迎参与）

以下功能**尚未实现**，是接下来几个版本的重点方向。它们的设计同样存在大量取舍，欢迎在 [Discussions](https://github.com/nexhub-app/Nexhub/discussions) 或 Issues 提意见：

1. **接入 AI 能力**：在应用内引入 AI 辅助，例如智能搜源、内容摘要、看番/看书助手、自然语言检索等。
2. **小说翻译**：为小说阅读器接入机翻管线，支持原文 / 译文对照、按需段落翻译与缓存。
3. **漫画翻译（MTL）**：探索漫画图片的机翻嵌字 / 气泡替换，降低跨语言漫画的阅读门槛。
4. **视频实时翻译**：影视模块的字幕 / 实时字幕翻译，支持外挂与内嵌字幕的语言切换。
5. **增加其他同步方式**：在现有 Bangumi 同步之外，接入更多同步后端（如 AniList、MyAnimeList、Trakt、SIMKL、MDList 等），并支持跨后端双向同步与冲突策略可配置。

> 以上均为规划项，不含在本次发布中；具体落地顺序与形态会以社区反馈调整。

---

## ✨ 本次更新内容（v0.4.0 → v0.4.1）

### 🌸 Bangumi 同步增强（核心）

- **详情页一键同步**：在条目详情页直接「同步到 Bangumi」，可精确到**某一集 / 某一章**。
- **手动集 / 章同步**：支持对单集、单章手动触发同步，不再只能全量。
- **播放器单线路可选 + 同步弹窗多选**：剧集按**当前线路**过滤；同步弹窗按网站样式重做，可一次勾选多个集 / 章节批量同步。
- **增量同步 + 冲突解决**：云同步改为增量传输，新增冲突检测与**状态明细**（哪里同步了、哪里冲突了看得清）。
- **统一备份归档**：本地导入 / 导出与云同步重设计，备份包结构统一、可渐进恢复。
- **记住播放 / 阅读位置**开关补齐并生效。

### 🧩 源管理重构

- **源库订阅 / 挑选导入**：可从源库浏览、挑选并一键导入，不再只能手动粘贴 JSON。
- **全字段编辑**：源管理内支持对源 JSON 字段进行可视化编辑。
- **源登录修复**：修复部分源在「源管理」页登录态识别与刷新的问题。

### 🔞 年龄限制体系

- 完善的年龄限制策略；**18+ 源在默认设置下自动隐藏**，可手动开启。
- 新增**网络收藏**（收藏来源不再局限于本地导入）。

### 📚 阅读 / 漫画体验

- **漫画阅读器**：修复翻页与进度条不同步的问题。
- **暗色模式阅读体验**：修复暗色下阅读器背景与预览，阅读更舒适。
- **详情页**：选集 / 章节目录的日期跟随全局日期格式；收起态标题定位修复；Bangumi 评论文档与新功能整合。

### 🔧 其他修复与改进

- **视频嗅探页**：修复 release 包一直加载的兜底问题。
- **GitHub Issue 模板**：新增 bug / feature / docs 三类固定格式模板，反馈更规范。
- **CI 构建修复**：Android Gradle 仓库改为官方优先、阿里云兜底，解决海外构建失败（见上方「需先读」）。

---

## 🛠️ 构建与发布

- CI 四平台（Android / Windows / Linux / macOS）build 命令均注入 `--dart-define=BANGUMI_CLIENT_ID` 与 `BANGUMI_CLIENT_SECRET`（来自仓库 Secrets），官方 Release 包的 Bangumi OAuth 登录开箱可用。
- 本版修复了 v0.4.0 在海外 runner 上因仓库镜像顺序导致的 Gradle 构建失败，四平台均可正常出包。

---

## 📦 下载说明

- **Android**：APK 按 CPU 架构分包，绝大多数手机请选 **`arm64-v8a`**；很旧的机型选 `armeabi-v7a`；模拟器 / x86 平板选 `x86_64`。装错架构会提示「安装包无效」。
- **Windows**：`NexHub-setup-*.exe` 为安装包（支持中英文、非管理员安装），`NexHub-windows-x64.zip` 为免安装便携版。若被 SmartScreen 拦截，点「更多信息 → 仍要运行」。
- **Linux / macOS**：分别为 `.tar.gz` 与 `.zip`。macOS 首次打开需在「系统设置 → 隐私与安全性」中放行。

---

## ⚠️ 已知限制

1. **0.4.x 仍为预览版**，部分高级功能仍在打磨，遇到问题优先备份数据并到 Issues 反馈；
2. **ECH 暂不生效**：网络设置中的 ECH 界面已就绪，运行时未接通；**SNI 仅尽力生效**（受 Dart TLS 栈限制）；
3. **自建构建的 Bangumi OAuth 不可用**：未注入凭据时请改用 Access Token 登录，或自行到 [bgm.tv/dev/app/create](https://bgm.tv/dev/app/create) 注册应用（回调地址 `nexhub://oauth/callback`）；
4. **Bangumi 吐槽仅只读**；源站评论的可用操作取决于源 JSON 声明了哪些路由。

---

## 🙏 参与共创

- 反馈 Bug → [Issues](https://github.com/nexhub-app/Nexhub/issues)
- 路线规划 / 新功能想法 → [Discussions](https://github.com/nexhub-app/Nexhub/discussions)
- 写源 / 改引擎 → 欢迎 Pull Request

> NexHub 不内置任何内容源。首次安装后需要自行导入源，才能看到内容。
