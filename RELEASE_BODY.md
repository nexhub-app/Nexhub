# NexHub v0.4.4

> 四合一媒体聚合客户端（动漫 / 漫画 / 小说 / 影视）—— 源即插件 · 共创社区。

---

## ⚠️ 请先读这一段

本版是 **v0.4.3 的跟进版**，重点两大块：**UI 设计基准落地**（弹层限高合规化、语义状态色 token 化、间距 / 圆角统一收敛）与 **五项功能修复**（小说目录实时同步、本地导入漫画、本地文件管理、下载路径选择器防冻结、RSS 订阅优化）。

- **依然是 0.4.x 预览版**：相对稳定的 v0.3.1 仍是「求稳用户」的推荐版本；愿意试用新功能请先**备份数据**（设置 → 备份 / 导出，以及导出你的源 JSON）。
- **本次附带修复**：修复了上一版误删的平台工程文件（Windows 构建目录 / 弹幕库 / 测试 / l10n 产物），Windows 构建已恢复正常。
- 遇到问题请到 [Issues](https://github.com/nexhub-app/Nexhub/issues) 反馈，**附上复现步骤 + 所用模块（动漫/漫画/小说/影视）+ 源类型**。

### 数据兼容性（好消息）

经代码核查，**本版没有破坏性数据变更**：v0.4.3 的收藏、进度、书签、历史、分组、评分、短评**可直接沿用**；**旧的源 JSON 无需修改、无需重新导入**。

### 升级方式（Android）

本版**沿用与 v0.3.0 / v0.3.1 / v0.4.0 / v0.4.1 / v0.4.2 / v0.4.3 相同的签名密钥**，可以**直接覆盖安装**，无需卸载。

---

## 🔭 路线规划（规划中 · 欢迎参与）

以下功能**尚未实现**，是接下来几个版本的重点方向。它们的设计同样存在大量取舍，欢迎在 [Discussions](https://github.com/nexhub-app/NexHub/discussions) 或 Issues 提意见：

1. **接入 AI 能力**：在应用内引入 AI 辅助，例如智能搜源、内容摘要、看番/看书助手、自然语言检索等。
2. **小说翻译**：为小说阅读器接入机翻管线，支持原文 / 译文对照、按需段落翻译与缓存。
3. **漫画翻译（MTL）**：探索漫画图片的机翻嵌字 / 气泡替换，降低跨语言漫画的阅读门槛。
4. **视频实时翻译**：影视模块的字幕 / 实时字幕翻译，支持外挂与内嵌字幕的语言切换。
5. **增加其他同步方式**：在现有 Bangumi 同步之外，接入更多同步后端（如 AniList、MyAnimeList、Trakt、SIMKL、MDList 等），并支持跨后端双向同步与冲突策略可配置。

> 以上均为规划项，不含在本次发布中；具体落地顺序与形态会以社区反馈调整。

---

## ✨ 本次更新内容（v0.4.3 → v0.4.4）

> 相对 v0.4.3 共 9 个提交，覆盖 UI 设计基准、漫画阅读器缩放交互、功能修复与工程修复。

### 🎨 UI 设计基准落地（核心）

- **弹层限高合规化（12 处 + 播放器 9 处）**：所有 `showModalBottomSheet` 统一为 `isScrollControlled: true` + 限高 85% 屏高 + 滚动容器——书架长按菜单、章节排序 / 操作、收藏方式选择、文章阅读设置、阅读模式选择、图片长按菜单、线路选择、笔记列表、下载格式选择、导出目录选择等，窄屏不再显示不全。
- **语义状态色 token 化**：新增 `AppStatusColors`（`ok` / `warn` / `fail` / `containerOf`），替换 17 处硬编码颜色；SnackBar 传 `onInverseSurface` 自动适配深浅主题。
- **间距 / 圆角统一收敛**：新增 `spaceXxs(2)` / `radiusXs(4)` 档位，40 个文件 92 处魔法数字机械替换为 `AppTokens`（像素级不变）。
- **UI 优化八项**：
  - 窄屏标签不再拥挤：段宽不足时隐藏图标只留文字；源管理内层 Tab 可滚动；
  - 源列表「未登录」徽章（声明了登录能力但未登录的源）；
  - 播放方向默认横屏，进入播放页自动横屏、退出恢复，桌面 / 平板不强制转向；
  - 播放器 9 个弹层合规化；发弹幕横屏底部弹层 / 竖屏对话框；媒体信息 URL 加复制按钮；播放统计标签可换行；
  - 全屏退出残留修复：启动统一 `edgeToEdge`，底部导航栏不再被手势条遮挡；
  - 小说主题跟随改为三档（跟随应用 / 始终夜间 / 始终日间）；
  - **加载态刺眼修复**：阅读器加载指示器加当前阅读背景色底板，夜间模式不再闪白；
  - **弹层基准合规首批**：播放器长按倍速、更多菜单、投屏、定时关闭四个弹层补齐滚动与限高。

### 🖼️ 漫画阅读器缩放交互（专项修复）

- **双击三态**：缩小 → 放大 → 复原循环切换，无需进入设置。
- **触摸板捏合**：支持触控板捏合手势直接缩放。
- **放大后拖动平移与滚轮浏览分离**：放大状态下拖动平移浏览细节，滚轮仍用于翻页浏览，互不干扰。
- **长按与拖动不再冲突**：手势识别层修复，长按唤起菜单不会误触发放大拖动。
- 新增 `docs/UI_BASELINE.md` 设计基准文档（Material 3 + 莫奈动态色体系融合，含窄屏响应式适配规范）。

### 🔧 五项功能修复

- **小说目录实时同步**：详情页与阅读器的目录共用单一数据源，切换章节目录实时同步，不再各自维护。
- **本地导入漫画阅读**：导入的漫画图片目录视为一部漫画，按文件名顺序阅读。
- **本地文件管理**：本地导入的文件支持**删除 / 重命名**（导入历史列表 + 书架本地段均可用，长按 / 右键唤起）。
- **下载路径选择器防冻结**：下载路径设置持久化并立即生效；目录选择器放后台 isolate 执行，**Windows 不再冻结、不再报 0x80070057 / LateInitializationError**。
- **RSS 订阅优化**：订阅先入库、后台补全校验，消除添加订阅时的卡顿。

### 🛠️ 工程修复（重要）

- **恢复误删的平台工程文件**：修复上一版整理 gitignore 时误删的 Windows 构建目录（`windows/`）、Web 平台工程（`web/`）、工具脚本（`tools/`）、弹幕渲染库（`third_party/canvas_danmaku`，vendored MIT）、l10n 产物与测试文件——四平台构建恢复正常。
- **Windows 播放器冻结修复**：`windows/runner/main.cpp` 启用独立 UI 线程策略，修复 window_manager 全屏与引擎线程互锁导致的画面冻结（Flutter issue #176088）。
- **Windows 构建 UTF-8**：CMake 增加 `/utf-8` 编译选项，避免非 ASCII 注释在默认代码页下的 C4819 警告。
- **窗口管理器初始化前置**：`main()` 中 `windowManager.ensureInitialized()` 移至 `runApp` 之前，修复进入 / 退出阅读器画面冻结、F11 / Esc 全屏失效的问题。
- **应用图标全面更新**：所有平台启动图标统一替换为新图标（Android mipmap + 自适应图标、iOS / macOS AppIcon、Windows ICO、Web、Linux）。

---

## 🔞 年龄限制与免责说明

NexHub 默认开启**年龄限制保护**：标记为 **18+（成人 / mature）** 的源在默认设置下**自动隐藏**，不会出现在浏览、搜索与源列表中。如需访问，须在「设置 → 内容分级」中手动开启「显示限制级内容」，并确认你已年满法定成年年龄。

- **源作者责任**：请在源 JSON 中如实填写 `ageRating` 字段（`general` 全年龄 / `teen` 青少年 16+ / `mature` 成人 18+，支持 `all`/`16`/`r18`/`nsfw` 等别名，缺省 `general`）。应用仅依据该字段自动分级与隐藏，**最终的内容合规性与年龄适宜性由源的提供方与使用方负责**。
- **内容责任**：NexHub 为开源技术演示项目，本身**不提供、不存储、不中转任何内容**，所有内容均来自用户自行导入的源。对于源所提供内容的版权、合法性及适宜性，NexHub 不做任何形式的担保或背书。
- **合规提示**：若你所在地区法律禁止访问此类内容，或你未满法定成年年龄，请勿开启限制级内容显示。使用本软件即表示你已阅读、理解并同意仓库内完整的《免责声明》（依据中华人民共和国相关法律法规拟定）。

> 源字段完整说明见网站文档「源编写教程 · 源字段完整参考」一节，或仓库 `lib/core/models/plugin_config.dart`。

---

## 🛠️ 构建与发布

- CI 四平台（Android / Windows / Linux / macOS）build 命令均注入 `--dart-define=BANGUMI_CLIENT_ID` 与 `BANGUMI_CLIENT_SECRET`（来自仓库 Secrets），官方 Release 包的 Bangumi OAuth 登录开箱可用。
- Gradle 仓库顺序沿用 v0.4.1 的修复（`google()` / `mavenCentral()` 优先、阿里云兜底），四平台均可正常出包。

---

## 📦 下载说明

- **Android**：APK 按 CPU 架构分包，绝大多数手机请选 **`arm64-v8a`**；很旧的机型选 `armeabi-v7a`；模拟器 / x86 平板选 `x86_64`。装错架构会提示「安装包无效」。
- **Windows**：`NexHub-setup-*.exe` 为安装包（支持中英文、非管理员安装），`NexHub-windows-x64.zip` 为免安装便携版。若被 SmartScreen 拦截，点「更多信息 → 仍要运行」。
- **Linux / macOS**：分别为 `.tar.gz` 与 `.zip`。macOS 首次打开需在「系统设置 → 隐私与安全性」中放行。

---

## ⚠️ 已知限制

1. **0.4.x 仍为预览版**，部分高级功能仍在打磨，遇到问题优先备份数据并到 Issues 反馈；
2. **ECH 暂不生效**：网络设置中的 ECH 界面已就绪，运行时未接通；**SNI 仅尽力生效**（受 Dart TLS 栈限制）；
3. **自建构建的 Bangumi OAuth 不可用**：未注入凭据时请改用 Access Token 登录，或自行到 [bgm.tv/dev/app/create](https://bgm.tv/dev/app/create) 注册应用（回调地址 `nexhub://oauth/callback`）；
4. **Bangumi 吐槽仅只读**；源站评论的可用操作取决于源 JSON 声明了哪些路由。

---

## 🙏 参与共创

- 反馈 Bug → [Issues](https://github.com/nexhub-app/NexHub/issues)
- 路线规划 / 新功能想法 → [Discussions](https://github.com/nexhub-app/NexHub/discussions)
- 写源 / 改引擎 → 欢迎 Pull Request

> NexHub 不内置任何内容源。首次安装后需要自行导入源，才能看到内容。
