# NexHub v0.4.0

> 四合一媒体聚合客户端（动漫 / 漫画 / 小说 / 影视）—— 源即插件 · 共创社区。

---

## ⚠️ 请先读这一段：这是一次「大版本」更新

本版相对 v0.3.1 共变更 **93 个文件、新增约 2 万行代码**，一次性引入了 **Bangumi 同步、网络配置体系、收藏分组、评论系统、源登录鉴权、详情页整体重构** 六大块新功能，并删除了漫画 / 小说两套旧详情页改为统一实现。

**这意味着：本版很可能存在较多尚未被发现的 Bug，稳定性低于 v0.3.1。**

- 如果你把 NexHub 当作日常主力工具、且当前 v0.3.1 用得很顺，**建议观望几天**，等 v0.4.x 修补版本再升级；
- 如果你愿意帮忙试用，**强烈建议先备份数据**（设置 → 备份 / 导出，以及导出你的源 JSON），再升级；
- 遇到问题请到 [Issues](https://github.com/nexhub-app/Nexhub/issues) 反馈，**附上复现步骤 + 所用模块（动漫/漫画/小说/影视）+ 源类型**，这对定位问题帮助最大。

### 数据兼容性（好消息）

经代码核查，**本版没有破坏性数据变更**：

- 旧的收藏、阅读 / 观看进度、书签、历史 **可直接沿用**，无需清数据；
- 新增的「分组」「本地评分」「短评」都是追加字段，缺省值安全，旧数据读取不受影响；
- **旧的源 JSON 无需修改、无需重新导入**，新增的 `network` / `comments` 字段全部可选。

### 升级方式（Android）

本版**沿用与 v0.3.0 / v0.3.1 相同的签名密钥**，可以**直接覆盖安装**，无需卸载。仅当你仍停留在更早的随机签名旧包时，才需要先卸载一次。

---

## 🗳️ 征求意见：Bangumi 集成该怎么做？（重要）

Bangumi 同步是本版最大的新功能，但它的设计涉及大量取舍——**同步方向、冲突处理、隐私边界、绑定策略**，目前的方案是我一个人拍板的，未必符合大多数人的使用习惯。

**我们计划在 GitHub Discussions 开启一个专门的讨论帖，想听听你的意见（发布后会置顶，链接稍后补充）：**

👉 **[GitHub Discussions · NexHub](https://github.com/nexhub-app/Nexhub/discussions)**

> 若你看到本说明时讨论帖尚未出现，可在 Discussions 新建一个「功能建议 / 设计讨论」分类的帖子，把下方 6 个问题贴进去即可；完整正文见仓库 `BANGUMI_DISCUSSION_POST.md`。

特别想听到你对以下几点的看法：

1. **同步方向**：目前是「以本地推送到 Bangumi 为主，只在本地空缺时回拉评分 / 短评」。你希望支持完整双向同步吗？冲突时以谁为准？
2. **自动 vs 手动**：现在需要手动点「立即同步」或在详情页单条同步。你希望「看完一集自动上报」吗？还是觉得自动上报太激进？
3. **条目绑定**：现在按标题相似度自动匹配（≥0.85 才自动采用），否则让你手选。这个阈值合适吗？误绑定的情况多不多？
4. **隐私边界**：默认是否应该把本地分组名作为标签推送到 Bangumi？新建收藏默认公开还是私有？
5. **该不该扩展**：影视模块要不要也接 Bangumi？还是接别的库（如 TMDB）更合适？
6. **登录方式**：OAuth 授权 vs 手填 Access Token，你更倾向哪种？

即使只回复一句「我只想要 XX」，也对后续设计非常有帮助。

---

## ✨ 本次更新内容（v0.3.1 → v0.4.0）

### 🌸 Bangumi 集成（本版核心）

打通 NexHub 与 [Bangumi 番组计划](https://bgm.tv) 的账号与进度同步。

**两种登录方式**

- **OAuth 2.0 授权登录**（推荐）：跳系统浏览器授权后自动回跳应用，带 refresh_token 自动续期，无需手动维护凭据；
- **手填 Access Token**：粘贴个人 Token，保存前会先校验有效性。

> ℹ️ OAuth 的应用凭据由**编译期注入**、不写在源码里。官方 Release 包已通过 CI 注入，开箱即用；**若你自行从源码构建**，未配置凭据时 OAuth 按钮会提示「未配置」，此时请改用 Access Token 方式，或到 [bgm.tv/dev/app/create](https://bgm.tv/dev/app/create) 注册自己的应用（回调地址填 `nexhub://oauth/callback`）后用 `--dart-define` 注入。

**同步能力**

- 同步 **收藏状态**（想看 / 在看 / 看过）、**动漫已看集数**（增量标记，只标差集）、**漫画 / 小说阅读进度**、**本地评分**与**短评**；
- 同步策略保守，**不会破坏你在 Bangumi 上的既有数据**：状态只进不退、进度只增不减，不覆盖远端的「搁置 / 抛弃」；仅当本地无评分 / 短评而远端有时才回拉写入本地；
- 支持按模块**分别开关**（动漫 / 漫画 / 小说）、**全量同步**、**详情页单条同步**，以及**从 Bangumi 导入**（按标题相似度自动绑定已有条目，不会凭空新建本地收藏）；
- 可选把本地分组名**作为标签一并推送**，也可将新建的收藏设为**私有**。

**详情页新增 Bangumi 标签页**

整页展示：封面、评分与评分人数、五种收藏状态统计、简介（可展开）、集数与放送日期、全部标签、角色、关联作品、吐槽；并提供「网页打开」「手动绑定」「同步状态覆盖」入口。

**条目绑定**

本地条目 ↔ Bangumi 条目的映射：优先用缓存，其次按标题相似度自动匹配（≥0.85 自动采用），否则弹出候选列表由你手选；支持随时解绑与手动指定收藏状态。

**代理 / 镜像设置**

针对 bgm.tv 直连不畅、封面加载不出的情况，新增「直连 / 镜像」两种模式，镜像下可**分别指定主站域名、API 域名、图片域名**。

---

### 🌐 网络配置体系（全局 + 源级覆盖）

新增独立的「网络设置」页，让你在应用内直接处理 DNS 污染、站点连不上、需要走代理等问题。

**全局可配置**

- **代理**：直连 / 跟随系统 / 手动指定（支持 HTTP 与 SOCKS5，可填账号密码，密码单独加密存储）；
- **DNS**：系统 / 自定义 UDP / **DoH（DNS over HTTPS）** / **DoT（DNS over TLS）**，内置 Cloudflare、Google、Quad9 预设，可开启解析缓存；
- **自定义 Hosts**：直接把域名钉到指定 IP；
- **SNI / ECH**：面向特殊网络环境的进阶选项。

**源级覆盖**

在源管理里进入某个源的「网络覆盖」页，可对 **代理 / DNS / SNI / ECH / Hosts** 逐项选择「继承全局」或「单独覆盖」。生效优先级：**用户覆盖 > 源 JSON 的 `network` 块 > 全局设置 > 默认值**。覆盖只作用于该源的抓取。

**实际效果**

全局网络配置会覆盖**整个应用**的网络请求（包括封面图加载、下载器等），DNS 解析带 TTL 缓存并优先命中自定义 Hosts。**修改后即时生效，无需重启应用。**

> ⚠️ 两个诚实的限制：**SNI** 受 Dart TLS 栈限制，仅能尽力生效；**ECH** 界面已完整，但**运行时暂未真正生效**，属于提前放出的占位入口。

**内置连通性测试**：代理测试、DNS 测试、DoH 测试，配完可以当场验证。

---

### 🗂️ 收藏分组（分类管理）

- 一个条目可以同时属于**多个分组**（标签式），与源站自带的题材分类互不干扰；
- **按模块隔离**：动漫 / 漫画 / 小说的分组各自独立，不会互相串台；
- **可隐藏**：不常用的分组可以隐藏，不再出现在分组栏和筛选里，随时能在「分类管理」中恢复；
- **多选筛选**：分组栏可同时选中多个分组做并集筛选（命中任一即显示）；给条目指定分组时也是多选；
- 书架顶部新增分组栏：[全部] [未分组] [各分组]，点击自动居中；「管理」面板支持**拖拽重排、重命名、删除、新建**（删除分组只解除关联，不会删掉条目）；
- **书架筛选新增「分组」维度**，与既有的排序 / 分类 / 状态 / 进度筛选叠加使用。

### ⭐ 本地评分与短评

可给任意条目打 **0–10 分**并写一句短评，入口在详情页 Bangumi 面板与书架卡片长按。评分 / 短评保存在本地，同时可作为 Bangumi 同步的一部分推送。

---

### 💬 评论功能

详情页新增「评论」标签，分两个子页：

- **网站评论**：来自源站，**支持发表评论、回复、点赞、举报**——但具体能做哪些操作，取决于源 JSON 声明了哪些路由，未声明的按钮不会显示；
- **Bangumi 吐槽**：**只读**展示，走公开接口，无需登录也能看。

源未声明评论能力时，详情页只显示 Bangumi 吐槽子页。

---

### 🔑 源登录鉴权

对需要登录才能看内容的源：

- **网页登录**：内嵌 WebView 打开站点登录页，登录成功后自动捕获会话 Cookie；
- **手动粘贴 Cookie**：桌面端等无 WebView 场景的降级方式；
- 登录态自动识别与刷新，面板顶部直接显示当前登录状态；
- 支持**退出登录**：只清除该站点的会话，不影响其他源。

---

### 🧭 详情页重构（三模块统一）

- **三套详情页合并为一套**：漫画、小说、影视不再各写一份，统一走同一实现，行为一致、修 Bug 也只修一处；
- **改为 Tab 结构**：顶部 Hero 大图（滚动时收起吸顶）+ 常驻主操作按钮（续看 / 续读，首屏就能点）+ 吸顶标签栏「详情 / Bangumi / 选集（章节目录）/ 评论 / 推荐」；
- **选集一次点击直达**：旧版需要点开折叠行或弹窗才能选集，现在是一个标签页；
- 重构后**原有能力一项未减**。

### 🎨 其他体验改进

- **统一的批量下载选集弹窗**：三个模块共用同一实现，影视会自动按线路去重再统计总数（并修复了旧实现的控制器泄漏）；
- **骨架屏加载**：列表加载时显示微光占位，多个卡片相位错开，避免整屏同频闪烁；
- **进度卡片**：全部读完 / 看完时高亮完结态，进度数字改为补间滚动动画；
- 详情页与书架的若干窄屏布局修复。

---

### 🧩 给源作者：源 JSON 新增两个可选字段

本版为「源即插件」体系新增两个**顶层可选字段**，不写就完全按原样工作，**旧源无需任何改动**。

| 字段 | 说明 |
| --- | --- |
| `network` | 源级网络覆盖块。子键：`proxy` / `dns` / `hosts` / `sni` / `ech`。缺省即继承全局设置；填了非法值只告警、不会导致源无法启用。 |
| `comments` | 声明该源的评论能力。 |
| `comments.provider` | 默认 `source`。`bangumi` 值目前**仅做解析，本期未实现**。 |
| `comments.routes` | 只有 `list` 是必需的；`replies` / `post` / `reply` / `like` / `report` 均可选，未声明则对应按钮不渲染。 |
| `comments.selectors` | `items` / `commentId` / `author` / `avatar` / `content` / `time` / `likeCount` / `replyCount` / `hasMore` / `success` 等，与顶层 `selectors` 用同一套引擎（JSONPath / CSS / XPath）。 |
| `comments.login.url` | 需要登录时的 WebView 登录页地址。 |
| `comments.login.checkCookie` | Cookie 中出现该键名即视为已登录。 |
| `comments.login.checkUrl` + `loggedInSelector` | 可选的二次探测，用于确认登录确实有效。 |

评论路由支持 `comments.` 命名空间，占位符替换、镜像切换、相对路径补全的规则与主路由完全一致。

---

## 🛠️ 构建与发布

- CI 补齐 **Bangumi OAuth 凭据的编译期注入**（Android / Windows / Linux / macOS 四平台），官方 Release 包的 OAuth 登录开箱可用。

---

## 📦 下载说明

- **Android**：APK 按 CPU 架构分包，绝大多数手机请选 **`arm64-v8a`**；很旧的机型选 `armeabi-v7a`；模拟器 / x86 平板选 `x86_64`。装错架构会提示「安装包无效」。
- **Windows**：`NexHub-setup-*.exe` 为安装包（支持中英文、非管理员安装），`NexHub-windows-x64.zip` 为免安装便携版。若被 SmartScreen 拦截，点「更多信息 → 仍要运行」。
- **Linux / macOS**：分别为 `.tar.gz` 与 `.zip`。macOS 首次打开需在「系统设置 → 隐私与安全性」中放行。

---

## ⚠️ 已知限制

1. **本版为大版本更新，Bug 数量预计高于以往**，请优先备份数据；
2. **ECH 暂不生效**：设置界面已就绪，运行时未接通；
3. **SNI 仅尽力生效**：受 Dart TLS 栈能力限制；
4. **自建构建的 OAuth 不可用**：未注入凭据时请改用 Access Token 登录；
5. **Bangumi 评论仅只读**；源站评论的可用操作取决于源 JSON 的声明。

---

## 🙏 参与共创

- 反馈 Bug → [Issues](https://github.com/nexhub-app/Nexhub/issues)
- **Bangumi 设计意见 → [Discussions](https://github.com/nexhub-app/Nexhub/discussions)**
- 写源 / 改引擎 → 欢迎 Pull Request

> NexHub 不内置任何内容源。首次安装后需要自行导入源，才能看到内容。
