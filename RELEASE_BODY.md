# NexHub v2.0.0-beta.3（预发布测试版）

> 四合一媒体聚合客户端（动漫 / 漫画 / 小说 / 影视）—— 源即插件 · 共创社区。

---

## ⚠️ 本版说明（预发布 · beta 测试）

这是 **2.0.0 的预发布测试版（beta.3）**，不是正式稳定版。在 beta.2 的基础上，本次重点带来**漫画页翻译**与**视频字幕实时翻译**两大跨语言阅读能力，并对**更新通道、网络连通性、翻译稳定性**做了大量加固。它**仍在测试中**，可能有个别未发现的问题。

### 相对 v2.0.0-beta.2 的主要变化（亮点）

- **漫画页翻译**：翻页自动对当前页做 OCR + 翻译，译文气泡按版面叠加，按作品记忆、失败可重试。
- **视频字幕实时翻译**：字幕面板开关逐句翻译，无字幕时截帧画面 OCR 兜底，译文覆盖层可选显示原文。
- **翻译稳定性全套加固**：并发限流、指数退避、缓存上限、异常归一，快速翻页与瞬时抖动不再触发限流 / 丢句。
- **更新通道修复**：测试版通道现在能正确识别新版本；并支持 alpha / beta / rc 预发布逐级升级。
- **网络连通性增强**：运行时 SNI 控制（免 SNI 绕封锁）、DNS 解析后缀（社区源不含 IP）、源级网络覆盖自动沿用源自带配置、无头静默抓取跟随源网络并接入 Cloudflare 验证与 DNS 污染绕行、修复直连 TLS / 端口丢失。
- **源解析能力增强**：路由占位符偏移（{page-1} / {page+1}）、meta 批量预取、分类键名容错。

> 底层仍为 Flutter 3.47，构建链 Java 17 / AGP / Gradle / Kotlin 已对齐；最低 Android 仍为 7.0。

### 已知风险（请知悉）

- beta 版可能仍有个别 bug；漫画页翻译 / 视频字幕实时翻译依赖 AI 接口配置，未配置时回落通用翻译或不可用，升级如遇问题请反馈。
- 追求绝对稳定的用户可继续用 v2.0.0-beta.2，等正式版；想抢先体验的欢迎升级并反馈问题。

### 数据兼容性（好消息）

经代码核查，**本版没有破坏性数据变更**：beta.2 的收藏、进度、书签、历史、分组、评分、短评**可直接沿用**；**旧的源 JSON 无需修改、无需重新导入**。

### 升级方式（Android）

本版**沿用与历史版本相同的签名密钥**，可以**直接覆盖安装**，无需卸载。

> 已在使用 beta.2 的用户：本次修复了测试版通道「收不到新版本」的问题，升级后应用内更新检查将能正常提示 beta.3 及后续预发布。

---

## 🔭 路线规划（规划中 · 欢迎参与）

以下功能**尚未实现**，是接下来几个版本的重点方向。它们的设计同样存在大量取舍，欢迎在 [Discussions](https://github.com/nexhub-app/Nexhub/discussions) 或 Issues 提意见：

1. **增加其他同步方式**：在现有 Bangumi 同步之外，接入更多同步后端（如 AniList、MyAnimeList、Trakt、SIMKL、MDList 等），并支持跨后端双向同步与冲突策略可配置。

> 已实现：漫画页翻译（MTL）与视频字幕实时翻译已在 v2.0.0-beta.3 落地；小说翻译（AI 双语 / 段落翻译）、AI 辅助（内容摘要 / 章节配图）已在 v2.0.0-beta.2 落地；AI 搜源 / 自然语言检索仍在规划中。以上规划项不含在本次发布中；具体落地顺序与形态会以社区反馈调整。

---

## ✨ 本次更新内容（v2.0.0-beta.2 → v2.0.0-beta.3）

> 相对 beta.2 共 8 个提交。以下为面向用户的重点，完整逐条记录见 [RELEASE_NOTES](./RELEASE_NOTES.md)。

### 🌐 翻译（跨语言阅读）

- **漫画页翻译（漫画阅读器）**：翻页自动对当前页做视觉 OCR + 翻译，译文气泡按视口 / 宽高比 / 阅读模式映射叠加；按「作品 | 章 | 页 | 语言」缓存；长按菜单与阅读设置面板开关（按作品记忆），失败可重试。
- **视频字幕实时翻译（视频播放器）**：字幕面板开关（跨会话记忆），节流读取播放器字幕逐句翻译；无字幕时截帧画面 OCR 兜底；译文底部覆盖层可选显示原文；按「语言 | md5」持久缓存。
- **AI 设置页新增接口卡片**：漫画翻译 / 视频翻译各自可填接口（留空回落通用翻译），设置内搜索可达。
- **翻译稳定性全套加固**：
  - 漫画页翻译引入并发信号量（网络请求上限 2，缓存命中不占槽），快速翻页不再触发 429 限流；
  - 视频 OCR 兜底增加防重入标记，视觉请求未返回不再并发发起；
  - 字幕单句翻译指数退避重试（最多 3 次），瞬时网络抖动不再丢句；
  - 小说整章批量翻译增加预算保护（超阈值改分块），并在提示词声明实际段数；
  - 翻译缓存容量上限（各 5000 条），AI 设置页展示缓存条数 + 一键清空；
  - 翻译异常归一为「连接 / 超时 / 其他」三类，三个模块统一处理；
  - OCR 兜底增加轨道检测：有字幕轨时不再误触发画面 OCR；
  - 图片缩放收口到独立模块、正确释放解码器，自然尺寸改读图片头部，避免微缩误差。

### 🔄 更新通道

- 版本比较改为 SemVer：核心版本相同时按预发布段逐段比较，修复「beta.1 → beta.2 同核心迭代被判为已是最新」导致测试版通道收不到更新；
- 正式版 > 预发布，alpha < beta < rc 按 SemVer 排序，可逐级升级；
- 新增预发布识别（按 tag），稳定版通道过滤增加 tag 兜底；
- 测试版通道描述更新为含 alpha / beta / rc。

### 🛰️ 网络与连通性

- **运行时 SNI 控制（纯 Dart）**：默认 SNI 取域名；「-」表示免 SNI（握手不发送 server_name，绕 SNI 封锁的主力手段）；支持 `.example.com` 后缀通配按站点覆盖；仅对直连 https 生效；设置页新增「域名 → SNI 映射」编辑器与「测试 SNI 握手」按钮；SNI / ECH 文案据实改写。
- **直连 TLS / 端口修复**：https 直连补全 TLS 握手（此前返回裸 TCP，等于明文打到 443）；尊重显式端口（此前带端口源被连到 443）。
- **DNS 解析后缀**：源只声明后缀，运行时各设备用自己的 DNS 现算地址，配置文件不含任何 IP，便于公开发布社区源；缓存键与四模式查询改用「主机 + 后缀」，失败回退原主机，支持作用域精确命中 / 子域通配，IPv4 优先 + 同族轮换规避污染掺入的不可路由 IPv6；多条 Hosts 全部收集并轮换；修复源级网络覆盖保存时丢失后缀。
- **源级网络覆盖自动沿用源自带配置**：导入自带 network 块的源后无需重新配置；清除自定义覆盖回落源自带块（而非全局），信息卡提示已自动沿用。
- **无头静默抓取跟随源网络**：静默抓取跟随源 hosts / DoH / 代理；接入 Cloudflare 挑战检测与可见验证页，并绕行 DNS 污染，改善被拦截站点的连通。

### 🧩 源解析能力（面向源作者 / 共创）

- **路由占位符偏移**：`{page-1}` / `{page+1}`；不少站点分页从 0 起算而引擎从 1 计数，此前源只能自造变量名（占位被清空）表现为「第 1 页取不到 / 翻页重复」；仅对纯整数变量生效，负偏移夹到 0，非数值原样保留。
- **meta 批量预取**：源可返回 `__fetchUrls` 数组（配合 `__fetchConcurrency`，默认并发 6、上限 16），引擎并发抓取后把响应数组交给 `__processor` 同步处理；用于「一页只给 N 个入口、真实数据要逐个跟进」的站点，避免脚本侧数十次桥接往返撞上执行超时；单条失败该位置填 null，不中断整批。
- **分类键名容错**：分类读取兼容 title / name / type_name，此前源写 `name` 会解析出一排可点但无字的空白 Tab。

### 🐛 其他

- 文档与配套测试同步更新，覆盖上述翻译 / 更新 / 网络 / 源解析能力。

> 无破坏性数据变更：beta.2 的收藏 / 进度 / 书签 / 历史 / 分组 / 评分 / 短评可直接沿用，旧源 JSON 无需修改、无需重新导入。沿用同一签名密钥，可直接覆盖安装。

---

## 🔞 年龄限制与免责说明

NexHub 默认开启**年龄限制保护**：标记为 **18+（成人 / mature）** 的源在默认设置下**自动隐藏**，不会出现在浏览、搜索与源列表中。如需访问，须在「设置 → 内容分级」中手动开启「显示限制级内容」，并确认你已年满法定成年年龄。

- **源作者责任**：请在源 JSON 中如实填写 `ageRating` 字段（`general` 全年龄 / `teen` 青少年 16+ / `mature` 成人 18+，支持 `all`/`16`/`r18`/`nsfw` 等别名，缺省 `general`）。应用仅依据该字段自动分级与隐藏，**最终的内容合规性与年龄适宜性由源的提供方与使用方负责**。
- **内容责任**：NexHub 为开源技术演示项目，本身**不提供、不存储、不中转任何内容**，所有内容均来自用户自行导入的源。对于源所提供内容的版权、合法性及适宜性，NexHub 不做任何形式的担保或背书。
- **合规提示**：若你所在地区法律禁止访问此类内容，或你未满法定成年年龄，请勿开启限制级内容显示。使用本软件即表示你已阅读、理解并同意仓库内完整的《免责声明》（依据中华人民共和国相关法律法规拟定）。

> 源字段完整说明见网站文档「源编写教程 · 源字段完整参考」一节，或仓库 `lib/core/models/plugin_config.dart`。

---

## 🛠️ 构建与发布

- CI 四平台（Android / Windows / Linux / macOS）build 命令均注入 `--dart-define=BANGUMI_CLIENT_ID` 与 `BANGUMI_CLIENT_SECRET`（来自仓库 Secrets），官方 Release 包的 Bangumi OAuth 登录开箱可用。
- Gradle 仓库顺序沿用既有修复（`google()` / `mavenCentral()` 优先、阿里云兜底），四平台均可正常出包。

---

## 📦 下载说明

- **Android**：APK 按 CPU 架构分包，绝大多数手机请选 **`arm64-v8a`**；很旧的机型选 `armeabi-v7a`；模拟器 / x86 平板选 `x86_64`。装错架构会提示「安装包无效」。也提供 **`app-release.apk`** 通用包（单文件全架构，无需挑架构，体积较大）。
- **Windows**：`NexHub-setup-*.exe` 为安装包（支持中英文、非管理员安装），`NexHub-windows-x64.zip` 为免安装便携版。若被 SmartScreen 拦截，点「更多信息 → 仍要运行」。
- **Linux**：`NexHub-linux-x64.tar.gz` 为免安装版（解压即运行）；`NexHub-linux-x64.deb` 为 Debian / Ubuntu 系安装包；`NexHub-linux-x64.AppImage` 为单文件便携版（赋予可执行权限后双击运行）。
- **macOS**：`NexHub-macos.zip` 为免安装版，`NexHub-macos.dmg` 为磁盘镜像（挂载后拖入「应用程序」）。首次打开需在「系统设置 → 隐私与安全性」中放行。

---

## ⚠️ 已知限制

1. **ECH 暂不生效**：Dart TLS 栈无 ECH API、无可用插件，直连暂不可行；替代路径：代理模式选手动指向支持 ECH 的本地内核，或开启系统安全 DNS 由 WebView 路径自动启用 ECH。
2. **SNI 运行时可控但仍受栈限制**：现可在网络设置中配置「域名 → SNI 映射」与「免 SNI（绕 SNI 封锁）」，仅对直连 https 生效；走外部代理时由代理自理；部分站点仍需实测。
3. **自建构建的 Bangumi OAuth 不可用**：未注入凭据时请改用 Access Token 登录，或自行到 [bgm.tv/dev/app/create](https://bgm.tv/dev/app/create) 注册应用（回调地址 `nexhub://oauth/callback`）；
4. **Bangumi 吐槽仅只读**；源站评论的可用操作取决于源 JSON 声明了哪些路由；
5. 遇到问题优先到 Issues 反馈。

---

## 🙏 参与共创

- 反馈 Bug → [Issues](https://github.com/nexhub-app/Nexhub/issues)
- 路线规划 / 新功能想法 → [Discussions](https://github.com/nexhub-app/Nexhub/discussions)
- 写源 / 改引擎 → 欢迎 Pull Request

> NexHub 不内置任何内容源。首次安装后需要自行导入源，才能看到内容。
