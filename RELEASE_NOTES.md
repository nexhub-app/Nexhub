# NexHub v2.0.0-beta.2

> 四合一媒体聚合客户端（动漫 / 漫画 / 小说 / 影视）—— 源即插件 · 共创社区。

这是 NexHub 的四合一媒体聚合客户端（动漫 / 漫画 / 小说 / 影视）。NexHub **不内置任何内容源**，所有内容都来自你自行导入的「源」。

> ⚠️ **这是 2.0.0 的预发布测试版（beta.2）**：我们想尽早把这一大波体验升级交到大家手上，但**它仍处于测试阶段**。相对 2.0.0-beta.1 是一次以**小说 / 漫画 / 影视三大阅读器体验增强 + 大量实机修复**为主的大型更新，并新增了 AI 翻译 / 配图、云同步、便携格式解析等能力。
>
> - **已知风险**：beta 版可能仍有个别 bug；部分功能（如后台播放 / 画中画）依赖真机系统能力，需你本机验证。
> - **数据兼容**：经代码核查，本版**无破坏性数据变更**——v1.2.0 的收藏、进度、书签、历史、分组、评分、短评可直接沿用；旧源 JSON 无需修改、无需重新导入；沿用同一签名密钥，可直接覆盖安装。
> - **建议**：追求绝对稳定的用户可继续用 v1.2.0，等正式版；想抢先体验的欢迎升级并反馈问题。详见下方《更新日志（v1.2.0 → v2.0.0-beta.1）》。

## ✨ 核心特性

- **四合一聚合**：动漫、漫画、小说、影视在一个应用里统一管理、浏览、搜索、阅读与观看。
- **源即插件 · 共创模式**：解析能力完全由可导入的源 JSON 驱动，应用只提供「引擎」，不写死任何站点。
- **多引擎解析**：声明式（XPath / JSONPath / CSS）+ 脚本式（内置 JS 沙箱）自动调度。
- **首页多板块与动态筛选**：标签 / 分类「所见即所得」，自动按源结构生成。
- **本地内容导入**（v1.1.0 起）：小说支持 TXT / EPUB，漫画支持 PDF / CBR / RAR / 7Z / ZIP / CBZ 等归档与整文件夹导入（Android 支持 SAF 文件夹）；书架一键导入，离线也能看。
- **漫画阅读器**：双页 / 单页、翻页手势、缩放与滤镜、阅读进度持久化，并针对常见痛点做过专项修复。
- **小说阅读器**：字体、字号、行距、边距、主题、亮度等样式自定义，设置即时生效并持久化。
- **影视播放器**：基于 media_kit（libmpv）单一内核，配合 canvas_danmaku 弹幕渲染。
- **Bangumi 同步**（v0.4.0 新增）：收藏状态、已看集数 / 阅读进度、评分与短评同步到 Bangumi，支持 OAuth 授权登录。
- **网络配置**（v0.4.0 新增）：全局与源级的代理 / DNS（DoH、DoT）/ Hosts 配置，应对连不上与解析污染。
- **收藏分组与评论**（v0.4.0 新增）：多分组标签式管理、本地评分短评；详情页源站评论与 Bangumi 吐槽。
- **下载管理 / 历史与更新追踪 / Material 3 动态配色 / 多语言 / 跨平台**。

## 📌 通用升级说明

除各版本「特别说明」外，**所有版本均无破坏性数据变更**：收藏 / 进度 / 书签 / 历史 / 分组 / 评分 / 短评可直接沿用，旧源 JSON 无需修改、无需重新导入。

**签名密钥**：自 v0.3.0 起使用同一固定签名密钥（经 CI Secret 注入，不入库），可直接覆盖安装；更早的随机签名旧包需先卸载一次再装。v0.2.12 曾引入固定签名，v0.3.1 起改为 CI 注入（密钥本体一致）。

## 📝 更新日志（倒序 · 最新在前）

### 📝 更新日志（v2.0.0-beta.3 → v2.0.0-beta.4）

> ⚠️ **预发布测试版**。相对 beta.3 共 20 个提交，以**跨语言翻译可靠性与可用性 overhaul**为主，并对**网络连通性、收藏、下载、更新通道、源库**做了大量加固。无破坏性数据变更，可直接覆盖安装。

### 🌐 翻译（跨语言阅读）
- **翻译接口主备容灾 + 健康熔断**：主端点失败自动切备用端点，连续失败临时熔断并恢复探测，降低 AI 接口偶发 5xx / 超时导致的整段失败（1764607）。
- **章节翻译断点续译**：按块写入检查点，中断后可从断点继续，避免重翻整章（532c7c9）。
- **术语表注入 + 统一提示词管线**：专有名词跨段一致，多书共享术语表；翻译提示词统一收口（9c18011）。
- **可选润色 + 证据驱动审校**：译文可选二次润色，关键句附原文证据便于核对（1c59adc）。
- **书籍预扫描**：进翻译前生成章节摘要 + 全书总览注入，长书上下文更连贯（5204e53）。
- **可复用译文资源导出**：译文导出为可复用资产（f59fb72）。
- **漫画页翻译气泡回填渲染**：译文气泡按版面回填，翻页残留更少、覆盖更准（9e4893f）。
- **视频整片离线字幕翻译 + 双语导出**：整片字幕离线翻完并导出双语字幕（eba1dd7）。

### 🔄 更新通道
- 下载候选链降级重试、测速严格校验、自定义镜像健壮性增强（5b47eca）。上次 beta.3 修复测试版通道识别，本次修复候选链与测速。

### 🛰️ 网络与连通性
- **同站请求串行化 + 智能退避**（防限流）：per-host 异步串行队列，杜绝并发扎堆触发 RST；重试按错误性质区分——connectionError 补重试、connectionTimeout（限流挂起）单补 + 长退避，避免给已限流站点火上浇油；修复流式下载重定向连接泄漏（71bc7f1）。
- **网络收藏修复**：修复列表走错解析入口导致的空白（源声明 `listEntry` 走正确入口，acf1530）；应用内文件夹切换 + 选夹添加（9cbc49c）；错误不再静默吞，改为错误态 + 重试入口。
- **e-hentai 登录粘贴 Cookie 归一化**：粘贴任意常见格式都能正确登录并解析（4a41c5d）。

### 📚 收藏 / 下载
- **收藏修复**：详情页无法收藏、图片收藏持久化丢失修复；收藏弹窗新增「收藏本章」（d6ae661）。
- **下载修复**：遗留产物按条目隔离，meta 写入失败回退下载根目录（b6fc0f7）。

### 🧩 源库
- 年龄限制下源库自动隐藏 18+ 源，源管理移除隐藏提示；全应用补齐触感反馈（416282d）。

### 🐛 修复与加固
- 修复对话框 `TextEditingController` 退场动画期被释放导致的连锁崩溃（a3d29ae）。
- 视频后台播放通知初始化失败修复，移除 `demo_multi_line` 死引用（442ec0b）。
- 构建链升级 Gradle 9.1.0 / AGP 9.0.1 / Kotlin 2.3.20（36a0f93）。
- `gen_icons.py` 输出路径统一走 `out_path` 校验（13d3215）。
- 文档：新增「翻译进阶」章节与引导入口（03f2cc5）。

### 📝 更新日志（v2.0.0-beta.2 → v2.0.0-beta.3）

> ⚠️ **预发布测试版**。相对 beta.2 共 8 个提交，以**漫画页翻译 + 视频字幕实时翻译**两大跨语言阅读能力为主，并对**更新通道、网络连通性、翻译稳定性**做了大量加固。无破坏性数据变更，可直接覆盖安装。

### 🌐 翻译（跨语言阅读）
- **漫画页翻译**：漫画阅读器翻页自动对当前页做视觉 OCR + 翻译，译文气泡按视口 / 宽高比 / 阅读模式映射叠加；按「作品 | 章 | 页 | 语言」缓存；长按菜单与阅读设置面板开关（按作品记忆），失败可重试。
- **视频字幕实时翻译**：字幕面板开关（跨会话记忆），节流读取播放器字幕逐句翻译；无字幕时截帧画面 OCR 兜底；译文底部覆盖层可选显示原文；按「语言 | md5」持久缓存。
- **AI 设置页**：新增漫画翻译 / 视频翻译接口卡片（留空回落通用翻译），设置内搜索可达。
- **翻译稳定性全套加固**：漫画页翻译并发信号量（上限 2，缓存命中不占槽）避免快速翻页触发 429；视频 OCR 兜底防重入；字幕单句指数退避重试（最多 3 次）；小说整章批量翻译预算保护（超阈值分块）；翻译缓存容量上限（各 5000 条）+ 设置页展示条数 / 一键清空；异常归一为连接 / 超时 / 其他三类；OCR 兜底轨道检测避免误触发；图片缩放收口并正确释放解码器。

### 🔄 更新通道
- 版本比较改为 SemVer：同核心版本按预发布段逐段比较，修复测试版通道「beta.1 → beta.2 被判为已最新」收不到更新；
- 正式版 > 预发布，alpha < beta < rc 按 SemVer 排序，可逐级升级；新增预发布识别（按 tag），稳定版通道增加 tag 兜底；测试版通道描述更新为含 alpha / beta / rc。

### 🛰️ 网络与连通性
- **运行时 SNI 控制**：默认 SNI 取域名；「-」表示免 SNI（绕 SNI 封锁）；支持 `.example.com` 后缀通配按站点覆盖；仅对直连 https 生效；设置页新增域名 → SNI 映射编辑器与「测试 SNI 握手」；SNI / ECH 文案据实改写。
- **直连 TLS / 端口修复**：https 直连补全 TLS 握手；尊重显式端口（此前带端口源被连到 443）。
- **DNS 解析后缀**：源只声明后缀，运行时各设备用自身 DNS 现算地址，配置文件不含 IP，便于公开发布社区源；四模式查询改用「主机 + 后缀」，失败回退原主机，支持作用域命中 / 子域通配，IPv4 优先 + 同族轮换规避污染掺入的不可路由 IPv6；多条 Hosts 全部收集并轮换；修复源级网络覆盖保存时丢失后缀。
- **源级网络覆盖沿用源自带配置**：导入自带 network 块的源后无需重配；清除自定义覆盖回落源自带块（而非全局）。
- **无头静默抓取跟随源网络**：静默抓取跟随源 hosts / DoH / 代理；接入 Cloudflare 挑战检测与可见验证页，并绕行 DNS 污染。

### 🧩 源解析能力（面向源作者）
- **路由占位符偏移**：`{page-1}` / `{page+1}`；仅对纯整数变量生效，负偏移夹到 0，非数值原样保留；解决「第 1 页取不到 / 翻页重复」。
- **meta 批量预取**：源可返回 `__fetchUrls` 数组（配合 `__fetchConcurrency`，默认并发 6、上限 16），引擎并发抓取后把响应数组交给 `__processor` 同步处理；单条失败该位置填 null，不中断整批。
- **分类键名容错**：分类读取兼容 title / name / type_name。

### 🐛 其他
- 配套文档与测试同步更新，覆盖上述翻译 / 更新 / 网络 / 源解析能力。

> ⚠️ **beta 已知待做 / 未完全验证**：漫画页翻译与视频字幕实时翻译的效果依赖 AI 接口质量与网络环境；SNI / 免 SNI 在部分站点仍需实测；ECH 直连仍不可行（Dart TLS 栈限制）。升级如遇问题请到 Issues 反馈。

### 📝 更新日志（v2.0.0-beta.1 → v2.0.0-beta.2）

> ⚠️ **预发布测试版**。相对 beta.1 共 107 个提交，以**三大阅读器体验增强 + 大量实机修复**为主：小说阅读器补齐双页 / 翻译 / AI 配图 / 内容编辑 / 云同步 / 便携格式解析等；漫画阅读器新增夜览 / 睡眠定时 / 资源内存优化；播放器新增超分辨率 / 弹幕发送 / 缓存降级 / 错误重试 / 声明式菜单，以及系统画中画与投屏的完整生命周期、桌面端画中画等。本版为 beta 测试，可能有个别未发现的 bug，建议反馈问题。无破坏性数据变更，可直接覆盖安装。

### 📚 小说阅读器（大量新功能）
- **双页模式**：宽屏翻页模式下左右并排双页，如摊开的书，进度以左页为准。
- **整本页码**：页眉 / 页脚新增「整本页码」槽位，跨章累计全书页位（部分校准以 + 标注估算）。
- **双语 / 段落翻译**：接入 AI 翻译（OpenAI 兼容），原文 / 译文对照面板，译文按书缓存；导出时附 `_段落翻译` 附录。
- **AI 配置页**：翻译 / 配图等 AI 能力集中配置（模型、密钥、接口地址），双语 / 段落翻译设置并入同一页。
- **AI 章节配图**：根据章节内容生成插图，以插图行插入本章，重载即图文混排。
- **AI 阅读总结 / 内容摘要**：按全书或章节生成摘要，快速回顾剧情。
- **内容编辑**：阅读器内直接改正文并保存，已编辑章节带角标、可「恢复原文」。
- **墨水屏主题**：选中墨水屏预设时正文炭灰 + 朱批强调色联动，夜间自动回退亮色文字。
- **书源 JS 简繁函数**：源脚本可直接调用 `t2s()` / `s2t()` 做简繁转换（与阅读器正文转换同语义）。
- **压缩包批量导入**：支持 zip / cbz / rar / 7z 归档内 txt / epub 解包入库，自动解析书名 / 作者。
- **便携文档解析**：新增 Mobi / PDF 文本层解析（纯 Dart，无需额外依赖）；UMD 暂无可靠样例，暂明确提示不支持。
- **阅读进度细粒度云同步**：WebDAV 逐书单文件同步，退后台静默上传，冲突自动裁决。
- **EPUB 导出自定义模板 + WebDAV 上传**：自定义 CSS / 封面 / 简介，导出产物可一键上传到 WebDAV。
- **划线 / 高亮 / 摘录 / 分享**：选区高亮 + 摘录笔记 + 文艺分享卡片；支持换源重定位，随 EPUB / TXT 导出附带。
- **在线多音色朗读**：可自定义 HTTP 朗读接口接入云端多音色；分句器按引号 / 冒号判旁白与角色，预下载 1–8 并发，失败以静音占位降级并自动续章。
- **朗读通知栏控制**：TTS 通知栏显示播放 / 暂停 / 上句 / 下句。
- **阅读中预下载**：读到阈值后后台预下载后续章节，离线可读。
- **平滑自动翻页**：可按间隔平滑推进，滚动推像素 / 翻页推过渡进度。
- **书签角标自定义图**：可为书签设置自定义角标图标。
- **书架未读角标 / 新章提示**；排序按最新章时间 / 中文拼音序。
- **繁简转换增强**：搜索标注与结果应用繁简转换，支持词组级最长匹配与排除词表；本地书文件名自动解析书名 / 作者。
- **长正文多页并发抓取**：目录多页（nextContentUrl）并发保序拼接，加快大章加载。
- **翻页动画与手势**：6 种动画、下滑切书签手势、音量键翻页。
- **滚动模式图文增强**：滚动正文两端对齐 + 插图 banner / card 与对齐。

### 🖼 漫画阅读器（增强 + 优化）
- **夜览暖色盖层**：独立开关 + 强度滑杆，暗光阅读更舒适。
- **睡眠定时**：按分钟（15–90）或按话数（1–3）双模式。
- **资源 / 内存优化**：归档临时目录退出回收、预载 / 渲染窗口淘汰、图片缓存按内存预算 + 磁盘清理入口、退后台冲刷进度防进程被杀、条漫解码限幅。
- **双击三态循环**、**E-Ink 刷新**、**ICC 校色**（6 种色彩预设）、**自动收藏封面**。
- **预加载数量可配（1–16）**、键盘快捷键补全、鼠标滚轮速度可调、鼠标光标反馈、放大态边缘滑动切页、缩放锚点可配、图片加载失败重试。

### ▶ 影视 / 播放器（增强 + 稳定）
- **超分辨率（Anime4K）**：效率 / 质量两档，无清晰度源时提升观感（按需开启，注意性能）。
- **系统画中画完整生命周期**：进入时记录播放位置、退出后续播，小窗三动作（播放 / 暂停、弹幕、快进），不满足条件不进入。
- **投屏完整生命周期**：投屏中与设备位置双向同步、错误可上报、断开自动暂停。
- **桌面端画中画**：改用缩小窗口置顶的小窗，可拖动，关闭后恢复原窗口。
- **弹幕发送上传**：登录弹弹play 后可发送弹幕（未登录就地登录，成功自动续发）。
- **缓存策略降级**：蜂窝网络 / 低内存设备自动降档缓存，切换即时生效。
- **错误分级重试**：分级超时 + 初始 / 切集自动重开自愈，减少卡死。
- **声明式播放器菜单**、**字幕按视频记忆**（外部字幕 / 样式按 URL 记忆）、**防重叠轨道算法**、**seek 后弹幕位置稳定**。
- 播放器抽象层与异步会话取消统一，播放页拆分为多个模块文件，提升稳定性与可维护性。

### 🔗 跨类型 / 书架 / 图库
- **跨作品待读队列**（小说 / 漫画）：阅读器三点菜单「加入 / 打开队列」，书架收藏长按也可加入，打开自动移出。
- **统一图片收藏图库**：播放器截图 / 小说插图 / 漫画分镜统一入库，缩略图带来源角标。
- **书架排序 / 分类筛选**：按最新章时间 / 拼音序排序，分类按实际格式派生并隐藏无文件项，支持手动拖拽。
- **图库**：文件夹 + 多选、预下载接入正式下载、筛选收起、手机适配。

### 🔐 源登录 / 验证
- **源登录 API Key 模式**：登录面板按源配置自动显隐账号 / 密码 / API Key。
- **内嵌 WebView 登录 + 手动获取 Cookie 回灌**：登录改内嵌浏览器，浮动按钮手动回灌 Cookie。
- **Cloudflare 验证修复**：验证域改用移动 / 完整浏览器 UA，修复 turnstile 600010。
- **弹弹play 新用户注册**：未注册用户可在应用内直接注册弹弹play 账号，与账号页整合。

### ⚙️ 设置 / 引导 / 更新
- **设置体系整理**：设置页结构重新归类，新增独立的「更新设置」页（更新检查 / 下载 / 提醒等）。
- **设置搜索补全**：内联设置弹窗的搜索也能命中全部具体设置项。
- **内容与源改名规则**：可自定义内容名 / 源名的改名配置与网络相关设置。
- **新手引导改版**：引导页内容与流程更新；操作时增加轻震动反馈。

### 🐛 其他修复与加固
- 封面偶尔丢失修复；条漫上翻跨章无缝衔接（不再瞬移 / 闪帧）。
- 本地漫画阅读进度全面恢复：切话 / 回目录 / 点击退出 / 重进四类入口都能续读。
- 加密源视频下载小概率解析失败修复；单章 / 单集下载进度显示修复；横屏上滑取消 seek 失效修复。
- 翻页模式正文溢出显示区、导致页底文字被裁的排版问题修复。
- 本地导入视频无封面修复；划线颜色自定义 / 工具栏溢出 / 点击退出等小说阅读器多项打磨。
- 分类首屏冷启动自愈并预载首个分类，避免弹错；历史记录「隐藏」软删除保留进度。
- 构建侧同步 CI 的 Flutter 版本并钉定 PDF 依赖版本。
- 大量播放器 / 漫画 / 小说阅读器交互与稳定性修复。

> ⚠️ **beta 已知待做 / 未完全验证**：超分辨率画质与性能档位、弹幕发送与 token 过期路径，以及投屏 / 画中画在部分机型与桌面环境的表现，仍需真机验证；进度条拖拽帧预览经评估暂不实现。升级如遇问题请到 Issues 反馈。

---

### 📝 更新日志（v1.2.0 → v2.0.0-beta.1）

> ⚠️ **预发布测试版**。相对 v1.2.0 共 44 个提交，是一次大型更新：底层升级到 Flutter 3.47、**小说 / 漫画 / 影视三大模块大量新功能与实机修复**、并新增 **Legado（阅读）格式书源导入兼容**。本版为 beta 测试，可能有个别未发现的 bug，建议反馈问题。无破坏性数据变更，可直接覆盖安装。

### 🛠 底层升级：Flutter 3.32.0 → 3.47.0
- 升级 Flutter / Dart 到 3.47 稳定版，构建链同步对齐（Java 17 / AGP 8.11.1 / Gradle 8.14 / Kotlin 2.2.20）；**最低 Android 版本仍为 7.0（API 24），不变**。
- 代码层 API 迁移（`withOpacity`→`withValues`、`surfaceVariant`→`surfaceContainerHighest` 等），Material 3 适配更规范。
- 升级后 Windows 桌面端增量构建缓存清理（字体 / 资源打包更稳）。

### 📚 小说阅读器增强（大量）
- **Legado（阅读）格式书源导入兼容**：支持导入 Legado 书源（JSON / XML / NDJSON），自动转成本地源 JSON；兼容其 `@css/@xpath/@json/@js`、`##` 正则替换、`@put` 变量、复合规则、目录 / 正文分页、分类（explore）复用搜索规则、`subContent` 分段正文、`formatJs/preUpdateJs`、`imageStyle/imageDecode` 等语法；网络导入对纯文本源（type=0）五链路（搜索 / 发现 / 详情 / 目录 / 正文）打通。
- **书内搜索重构**：结果携带章内字符偏移，跨章也能直接跳到命中页（不再只落到章节首页）；全书搜索由串行改 6 路并发并按章序归并，面板存活期内缓存已拉正文；段内一词多现逐条列出（此前每段只报首现）；单次结果上限 300 条防列表卡顿；正文与关键词都按阅读器繁简口径转换后再匹配（屏显简体、原文繁体时也能用简体词搜到）；搜索中显示「已搜索 N/M 章」进度。
- **阅读总结 / 速览**：新增离线抽取式摘要 + 云端 AI 摘要（模式可切），快速回顾全书脉络。
- **进度恢复修正**（重要）：翻页进度改为「章内字符偏移」，抗字号 / 边距变化；修复「翻到首页退出重进回退到上一章末页」；退出 / 切后台自动落盘，不再丢失位置。
- **音量键翻页**：默认开启（与漫画一致），手机侧键翻页。
- **滚轮翻页**：翻页模式下鼠标滚轮翻页（可反转方向）。
- **TXT 内部章节细化**：导入的 TXT 由「按双换行分段」改为逐行扫描，段落间只有一个换行的文本不再整本塌成一章；标题规则增强（英文 Chapter 支持阿拉伯数字与英文拼写数字、中文数字补「两」、「前言 / 终章 / 结语」等固定名）；标题行三重校验（非空、行长 ≤40、相邻间隔 ≥500 字）过滤误判；首个标题前的长正文独立为「前言」章；全书零命中时按约 10KB 在段落边界兜底硬切，超 100KB 单章二次切分；阅读器接入章节结构（与 EPUB 同构），目录 / 上下章导航 / 章节级进度与书签可用。
- **EPUB 导出内嵌插图**：导出的 EPUB 正确携带插图。
- **下载繁简转换**：下载产物应用简繁转换设置。
- **朗读（TTS）修复**：连续续读、点按段落立即切换、ListTile 断言洪水修复。
- **GBK / GB2312 站点乱码修复**：自动探测 HTML `<meta charset>`，gb2312 / gbk 站点书名、作者、章节标题与中文章节 URL 不再乱码（笔趣阁类站点可用）。
- **笔趣阁反爬兜底**：源声明 `useWebview` 时，被 Cloudflare 类拦截的页面改走真浏览器取渲染后 HTML 回灌解析。
- **标题行溢出 / 字号拖动防卡死、目录分卷分组**等细节打磨。
- **搜索历史**：记录并快速复用近期搜索词。
- **已下载章节标题显示修复**：下载到本地的小说不再把章节标题显示成内部文件名。
- **搜索按钮卡退修复**：阅读器搜索按钮在部分布局下触发崩溃的问题修复。
- **替换规则 / 高亮规则**：排版期正文净化（pattern / 替换 / 正则）与高亮（字色 / 背景 / 下划线），书籍级开关，变更不重拉全书。

### 🖼 漫画阅读器增强（大量）
- **书签**：为当前页加书签，书签列表可跳转 / 删除。
- **图片收藏图库**：长按 / 右键图片「保存」，归入图片收藏，集中查看。
- **自然排序**：本地漫画 / 归档按自然顺序（2.jpg < 10.jpg）正确排序，不再字典序错乱。
- **自动下载**：阅读中自动预下载后续内容（>阈值触发），离线更顺。
- **缩放倍数指示**：缩放时显示当前倍数。
- **顶栏章节链接绝对 URL**：顶栏章节地址按绝对 URL 判定，拼接更正确。
- **全屏快捷键仅绑 F11**：F 键不再抢占全屏，避免误触。
- **音量键翻页**：手机侧键翻页（与小说一致）。
- **连续阅读 7 项修复 + 多项实机修复**：本地漫画连续阅读、双击缩放三态、自动翻页 / 滚动、首屏单图进度、条漫导航连续性、自动滚动触顶卡顿、设置面板关闭丢改动等一大批实机问题修复。
- **聚合本地回上一话末页修复**：本地聚合漫画回上一话精确落末页。
- **双击缩放开关修复**：「双击缩放」设置项此前不响应开关，现已正确生效。
- **桌面端真实全屏与键盘快捷键**：window_manager 接管全屏（F11 进入 / Esc 退出）；方向键 / PageUp·Down 翻页、± 缩放、N·P 上下话；图片区右键呼出操作菜单。
- **缩放手势一组修复**：条漫未放大时也能双指捏合发起缩放（跨页同样生效）；放大后单指点击不再误触发翻页 / 导航；双击不再同时触发「翻页 + 缩放」；双击缩放锚点改为手指点击点而非屏幕中心；缩放回 1x 后清除残留平移偏移；放大后平移带边界夹紧；翻页前自动重置缩放。
- **进度写入可靠性**：退出阅读器时先把防抖中待写的进度落盘再取消定时器，不再丢最后几秒；恢复进度期间屏蔽滚动回写，避免污染存档。
- **亮度双轨**：亮度为正时写系统亮度，为负时把系统亮度压到最低再叠加黑遮罩，暗光下可读更低。
- **条漫负 margin 崩溃修复**：升级 Flutter 3.47 后条漫负 margin 触发框架断言崩溃，已修复。

### ▶ 影视 / 播放器增强（大量）
- **多源自动选源 + 故障回退**：进集 / 切集按记忆线路优先解析；某线路失败时自动轮换下一个候选并提示，全试过才报「链接失效」。
- **跨作品播放队列**：更多菜单「加入队列 / 下一部 / 队列管理」，跨作品自动连播；启动可恢复最近队列；支持本地 / 直连视频入队。
- **后台播放 + 通知栏**：接入前台媒体会话，锁屏 / 切后台继续播放并显示通知控制（音频类内容）。
- **画中画生命周期**：进 / 出画中画恢复位置与续播。
- **跳过片头 / 片尾**：自定义跳过区间，自动跳过。
- **自动连播倒计时**：一集结束前显示倒计时并自动连播。
- **睡眠按集数**：按剩余集数定时停止，而非仅按分钟。
- **双击 seek 累加**：连续双击累加快进 / 快退（±Ns 指示）。
- **底部细进度条**：控制栏隐藏时显示 2.5px 细进度条。
- **弹幕同文本去重**：5 秒窗口内相同弹幕合并，不再刷屏。
- **本地视频封面（文件夹优先 + 截帧）**：优先用文件夹封面 / 同名图，否则静音截首帧生成封面；Android SAF（content://）导入的视频也能生成封面。
- **横滑取消手势**：横滑 seek 中明显上下偏移可取消跳转，有触觉反馈。
- **m3u8 可下载**：HLS 流支持分段拼接 .ts 下载（变体流与初始化段已处理）；加密分片仍需密钥，保持已知限制。
- **控制栏隐藏租约**：拖动进度条或打开菜单 / 面板时「持有」控制栏，期间不自动隐藏，释放后重启计时。
- **双击防抖**：双击后 600ms 内屏蔽单击，避免三分区误触。
- **网络测速显示**：缓冲时实时显示网速（MB/s），无此能力自动隐藏。
- **本地 / 直链视频自动播放**：打开本地与直链视频不再停在暂停态，行为与在线路径一致。
- **横滑 seek 方向修正**：横滑方向此前与双击快进相反（右滑反而快退），现已统一。
- **画面比例真正生效**：容器不再固定 16:9，改为跟随比例设置，4:3 与填充模式视觉正确。
- **切集失败不再静默**：切集失败会回滚到原来的集并给出提示，不再停留在「新集索引 + 旧画面」。
- **退出前进度不丢**：退出 / 切后台时最后几秒的进度能正常保存（此前会静默丢失）。
- **睡眠定时到点状态同步**：到点后界面同步为暂停态，并取消控制栏自动隐藏。
- **错误清缓存重连与弹幕稳定**：加载失败附「清除缓存」动作避免过期直链假死；分级超时 + 切集自动重开自愈；弹幕 seek 后位置稳定、tick 性能优化不再卡顿。
- **播放器内部健壮性修复**：重试不再泄漏旧播放实例；初始化各阶段补齐生命周期复查；播放位置回调 250ms 节流避免整页重建；线路名为空时走统一翻译；投屏断连改为可等待，快速重进不残留会话；切集时的弹幕加载错误提示与首次加载一致。
- **退出 / 切换稳定性与弹幕崩溃修复**：播放器进入 / 退出稳定性、弹幕加载崩溃、退出后快速重进崩溃、播放中崩溃防御等一大批修复。
- **播放位置记录限流**：按作品维度只保留最近 50 条位置记录，避免长期积累膨胀。
- 画面比例、投屏握手超时回滚、线路切换恢复原位置、睡眠定时跨集保留、弹幕增量注入等交互打磨；弹幕源并行竞争取最快返回并缩短超时。

### ⬇ 下载 / 源 / 首页 / 导入
- **应用内版本升级**：设置内检查新版本、显示下载进度百分比、镜像列表增强。
- **下载进度中间值修复**：HLS 分片 / 直链 / 小说三类下载都能报告真实中间进度，不再只跳 0% 与 100%。
- **shuyuan（Legado）导入**：见上「小说阅读器」；非文本类型源（音频 / 图片 / 文件 / 视频）标为「类型不支持」不勾选。
- **PluginConfig author 字段**：源可声明作者信息。
- **书源合规去标示**：源格式兼容描述中性化。
- **笔趣阁 WebView 兜底**：见上「小说阅读器」。

### 🐛 其他修复与内部加固
- 新手引导、软件公告弹层适配小屏 / 横屏并加 SafeArea。
- 运行日志体系升级（分级 + 自动采集下载 / 解析 / HTTP 错误），shuyuan 链路补日志便于排查。
- 核心入口 / 导航 / 存储 / 服务整理；构建与工程配置稳定性提升。
- UI_BASELINE.md 移出 git 跟踪（保留文件）；RELEASE_NOTES 去重整理；文档瘦身（公开文档白名单）。

> ⚠️ **beta 已知待做 / 未完全验证**：后台播放 / 画中画的系统级行为需在真机验证；Legado 高级特性（如个别 JS 规则语法）可能仍需个别打磨；视频清晰度的手动切换、进度条帧预览暂未实现。升级如遇问题请到 Issues 反馈。

---

### 📝 更新日志（v1.1.0 → v1.2.0）

> 相对 v1.1.0 共 20 个提交，重点是**本地内容（下载 / 导入）体系稳定性大修**与**三大阅读器体验增强**：Android SAF 下载写入、本地视频 / 漫画 / 小说打开、大文件加载、EPUB 章节解析、下载落盘等此前反馈的痛点集中修复，并新增「运行日志」便于反馈排障。

### 📥 本地内容导入 / 解析增强
- 新增**文本编码自动识别**，提升本地 TXT / 无 BOM 文件的解析正确率，减少乱码。
- **EPUB 导入更稳**：目录文件（toc.ncx）不再混入正文；支持按 `class="chapter"` / `class="part"` 等常见写法切分章节，解决「整本只出一章」；Android SAF 文件无扩展名时按 MIME 类型兜底识别，文件夹为空的误判大幅减少。
- 本地文件夹扫描更健壮：单个子目录读取失败不再中断整次导入。

### ⬇ 下载管理增强 + Android SAF 文件系统
- **Android 下载路径**：改用系统文件夹选择器（SAF），默认下载目录在 Android 上正确可用；可在「设置 → 下载」重新选择。
- 修复 Android SAF 下载后**文件名被系统改写**（如 `.cbz` → `.cbz.zip`）导致下载内容打不开的问题；写入后自动回读校验、改名并清理残留文件。
- 修复下载任务 ID 含链接斜杠导致**写入 / 读取路径错乱**（重下即可生效）。
- 下载失败不再「看不见」：失败任务在下载列表正常显示，并新增**「运行日志」页**（设置 → 高级）集中查看下载 / 解析 / 网络错误，反馈 bug 时可一键复制。

### 📚 小说阅读器增强
- **大 EPUB / TXT 加载卡顿大幅改善**（移除分页热路径瓶颈），超大章节也能流畅翻页。
- **本地小说下载落盘改造**：改为**逐章独立 TXT 文件 + 插图 images/ 子目录**，根治「分两批下载只显示一章」；默认下载格式由 EPUB 改为 TXT（更通用、可直接打开）。
- 本地小说点击统一走文件夹扫描阅读，逻辑更一致；EPUB 章节跳转更顺。

### 🖼 漫画阅读增强
- 本地漫画 / 图片导入与阅读体验打磨（阅读页操作、看图菜单等）。

### ▶ 影视 / 播放器增强
- 修复**本地下载视频（含 Android SAF / HLS .ts）一直加载、无法播放**的问题。
- 视频下载改为**「先嗅探真实地址、再下载」**；修复 HLS 分片下载与单分片超时，下载成功率更高。
- 影视首页浏览 / 搜索体验打磨。

### 🔌 抓取 / 源 / 首页与导入流程
- **HTTP 抓取增强**：同源 Referer 兜底等，提升防盗链站点的连通与下载成功率。
- **源即插件系统增强**：源加载 / 图片 / 管理面板更稳定。
- **首页与导入流程增强**：本地导入入口、本地文件查看器（图片 / 视频 / 文本）体验打磨。
- 书架组件修复。

### 🐛 其他修复与内部加固
- 修复用户反馈的多个问题。
- 新手引导、软件公告弹层适配小屏 / 横屏，并加 SafeArea 防止被导航栏 / 刘海遮挡。
- 运行日志体系升级（分级 + 自动采集下载 / 解析 / HTTP 错误），反馈排障更轻松。
- 核心入口 / 导航 / 存储 / 服务整理；构建与工程配置稳定性提升。

---

### 📝 更新日志（v1.0.0 → v1.1.0）

本版相对 v1.0.0 共 7 个提交，核心是**本地内容导入体系**：小说 / 漫画离线内容从「仅图片与 TXT」扩展为 **EPUB / PDF / 多格式漫画归档 / 整文件夹 / Android SAF** 的完整导入链路，同时**设置搜索升级为 226 项全覆盖 + 精确滚动定位**。

### 📥 本地小说：EPUB 支持（核心）
- 小说阅读器新增 **EPUB** 导入：应用内直接解析 OPF / XHTML 生成章节结构，无额外依赖；TXT 继续支持。
- 导入路由修正：.txt / .epub 正确分派到小说阅读器，章节切换、进度保存与在线章节一致。

### 🗂 本地漫画：PDF + 多格式归档 + 文件夹聚合（核心）
- **PDF 漫画**：每页渲染成图片复用现有漫画看图管线（`pdfx` 原生引擎），首次打开逐页渲染、之后复用缓存；支持封面预览与按页序阅读。
- **漫画归档多格式**：新增 `.cbr / .rar / .7z / .cb7 / .cbt` 支持（纯 Dart 解压，含 RAR5），原有 `.cbz / .zip` 继续可用；按图片路径自然排序，页码顺序正确；单张图片解压上限保护防解压炸弹。
- **文件夹导入聚合**：选择目录后询问「合并为整部（文件夹 = 一部作品，内部文件 = 章 / 话）」或「逐文件分别导入」；聚合条目复用在线章节导航（目录 / 上下章）。
- **Android SAF 文件夹导入**：解决 `file_picker` 在 Android 返回 `content://` 目录无法读取的问题——递归枚举目录树、缓存为本地文件后复用全部读取管线，选中目录即能导入。
- 漫画 / 影视 / 小说三个书架「本地」分段顶栏导入入口常驻，随时导入不迷路。

### 🔍 设置搜索：226 项全覆盖 + 精确滚动定位
- 设置项注册表覆盖 **226 条真实设置项**，搜索结果不再只跳到设置页，而是**直接滚动到具体那一项**（如搜「主题色」直达自定义取色器那一行）。
- 新增滚动定位机制（`SettingsAutoScroll`）：懒列表自动扫掠构建锚点，命中后平滑滑入并叠加高亮脉冲，增强「灵动感」。

### 🔧 其他体验与内部加固
- Bangumi OAuth 授权对话框增加加载进度条与圆角样式，加载状态更直观。
- 软件公告服务与设置页内部逻辑梳理，减少重复渲染。
- 清理 .gitignore 重复条目；公告 ID 随版本更新。

---

### 📝 更新日志（v0.5.1 → v1.0.0）

本版相对 v0.5.1 共 1 个提交，重点是**新手引导扩展、软件公告系统、Bangumi 登录重写修复**，以及若干界面收敛与体验优化。

### 🧭 新手引导（Onboarding）扩展为 6 页（核心）
- 首次启动走完引导再进主页：欢迎 → 添加源 → 关联 Bangumi → 隐私与合规 → 基础设置（外观主题 / 界面语言）→ 授予 Android 权限。
- 引导内含隐私声明：「你的源、凭据与浏览记录只保存在本机，不上传任何服务器；应用不写死任何密钥。」

### 📢 软件公告系统（全新）
- 远程公告经 jsDelivr CDN 加速（失败自动回退官方源），首帧后弹出未读公告；新增 `SoftwareAnnouncementService` / `SoftwareAnnouncementDialog` 与 `assets/announcements.json`。

### 🔐 Bangumi OAuth 登录重写（修复卡死）
- 从独立内置浏览器窗口改为嵌在对话框里的 WebView；修复「授权后返回时回调丢失、界面一直转圈」的故障。

### 🖥️ 浏览器窄屏底栏收窄
- 窄屏底栏改用纯 Container，高度可控（40px）+ 1px 顶部分隔线，5 个按钮带 tooltip。

### 📥 源管理 / 采集 API 入口收敛
- 采集 API 入口收进「网络导入」Tab 搜索框，移除悬浮按钮与多余内联按钮。

### 🧪 测试
- 新增漫画解析器测试（`test/manga_311s_parser_test.dart`）。

---

### 📝 更新日志（v0.5.0 → v0.5.1）

本版相对 v0.5.0 共 2 个提交，重点是**源管理与收藏流程重构**，一批**源能力增强**（库源一键更新 / 镜像测速 / 镜像自动获取 / 评论兼容 / 脚本文本预取），以及若干崩溃修复与体验优化。

### 📚 源管理 / 收藏流程重构（核心）
- **拖动排序灵动动画**：源管理器与设置 Hero 页拖动排序改用 `proxyDecorator`（缩放 + 主题色描边 + 双层阴影）。
- **源磁贴 Material 崩溃修复**：统一包裹透明 `Material`，修复部分页面运行时 `No Material widget found` 崩溃。
- **单源搜索横向列表**：启用鼠标 / 触控板拖动；媒体 / 小说 / 漫画主页隐藏采集 API 浮动按钮避免遮挡；库源 / 在线内容 / 在线浏览 / 镜像等页面多项交互与布局优化。

### 📦 库源一键更新（全新）
- 库源支持版本比对与一键更新，状态覆盖更新中 / 已更新 / 更新失败 / 全部最新。

### 🌐 镜像增强（全新）
- 镜像页自动从源 `mirrors` 路由获取并添加可用镜像（主域失效也能回退内置备用域名）；新增「测速全部」。

### 💬 评论兼容增强
- 兼容以 id 为键的对象形评论列表（如再漫画）；Unix 秒 / 毫秒时间戳自动格式化为绝对时间。

### 🧩 脚本解析器增强
- 新增 `__fetchResponseType`（json / text）文本预取；`mirrors` 路由返回 `[{name,domain,baseUrl}]` 原样透传，保持源即插件原则。

### 🔧 编译与工程修复
- 修复 `PointerScrollEvent` 导入缺失、`lerpDouble` 调用、ReorderableListView 括号等编译错误；补齐新功能所需 l10n 文案。

---

### 📝 更新日志（v0.4.5 → v0.5.0）

本版是 v0.4.5 的**功能增量版**，相对 v0.4.5 共 5 个提交，引入两块全新能力并重做设置与播放器设置。

### 📊 阅读统计页（全新）
- **阅读时长追踪**：`ReadingSessionRecorder` 在视频 / 漫画 / 小说阅读器内实时记录会话时长，自动聚合到按日、按作品。
- **统计页**：设置 → 统计，三 Tab（总览 / 按日 / 列表）+ 搜索 + 汇总卡，按作品展示累计阅读时长与最近活跃。
- **热力图日历**：GitHub 风格日网格，按月份切换、null 表示「全部」，直观呈现每日 / 每月阅读密度。

### 🐞 调试 / 崩溃日志（全新）
- 新增 `crash_log` 模块与「崩溃日志」页（设置 → 高级 / 关于入口），集中展示运行期异常堆栈，便于向 Issues 附准确日志；顺带清理死代码、规范日志输出。

### 🎛️ 设置页重构
- **设置分类化**：拆分出「高级」「外观」「分类管理」独立页面，设置主页结构更清晰。
- **阅读器全局设置补全**：小说 / 漫画 / 弹幕三模块「全局默认设置」页打通并清理冗余控件——小说补「繁简转换 / 正文字体族」，漫画 / 弹幕设置项归一；媒体库等用户可见文案同步打磨；设置页整体加灵动动效。

### 🎬 播放器设置打通全局 + 剧集覆盖
- 播放器设置打通「全局默认 + 单作品（per-episode）覆盖层」：解码 / 音频 / 比例 / 倍速 / 连播 / 长按开关 / 长按倍速 7 项可在作品级覆盖、无覆盖回落全局；弹幕显示设置清理冗余控件。

### 🖼️ 条漫反向翻页修复
- 修复条漫末页继续上翻时的反向翻页判定，配合上一版落点修复，上下翻页均稳定。

### 🔧 备份 / 自动同步修复
- 修复自动同步定时器与备份归档的边界问题，备份 / 同步更可靠（统计盒已纳入备份白名单）。

---

### 📝 更新日志（v0.4.4 → v0.4.5）

本版是 v0.4.4 的**紧急修复跟进版**，相对 v0.4.4 共 2 个提交，聚焦影视播放器与条漫阅读器的体验修复。

### 🎬 影视播放器修复
- **视频退出横屏恢复**：修复从全屏播放退出后屏幕方向残留横屏、未恢复进入前方向的问题。原逻辑仅在「经过全屏」时才还原方向，而进入播放页默认锁定横屏（`lockOrientation: landscape`），导致退出后方向被锁死；现改为退出播放页时**无条件还原方向**（`SystemChrome.setPreferredOrientations([])`），桌面 / 平板不受影响。

### 🖼️ 条漫（Webtoon）阅读器修复
- **消除割裂感**：图片加载完成后移除占位约束、按真实图高显示，根治长条图拼接处的「割裂 / 跳变」观感（`SourceImage` 新增 `onLoadComplete` 回调支撑占位移除）。
- **消除细白条**：修复图片之间的缝隙透出浅色背景、形成「细白条」的问题，长条图连续滚动不再露白。
- **选集界面手机端显示**：选集 `BottomSheet` 限高由 0.6 屏高放宽到 0.85 屏高，集数列表改为紧凑布局，线路改为横向 Chip 行——窄屏（手机端）下选集与线路正常展示，不再拥挤或显示不全。

---

### 📝 更新日志（v0.4.3 → v0.4.4）

本版重点三大块：**UI 设计基准落地**（弹层限高合规化、语义状态色 token 化、间距 / 圆角统一收敛）、**漫画阅读器缩放交互修复**与 **五项功能修复**（小说目录实时同步、本地导入漫画、本地文件管理、下载路径选择器防冻结、RSS 订阅优化）；同时恢复上一版误删的平台工程文件。

### 🎨 UI 设计基准落地
- **弹层限高合规化（12 处 + 播放器 9 处）**：所有 `showModalBottomSheet` 统一 `isScrollControlled: true` + 限高 85% 屏高 + 滚动容器——书架长按菜单、章节排序 / 操作、收藏方式选择、文章阅读设置、阅读模式选择、图片长按菜单、线路选择、笔记列表、下载格式选择、导出目录选择等，窄屏不再显示不全。
- **语义状态色 token 化**：新增 `AppStatusColors`（`ok` / `warn` / `fail` / `containerOf`），替换 17 处硬编码颜色；SnackBar 传 `onInverseSurface` 自动适配深浅主题。
- **间距 / 圆角统一收敛**：新增 `spaceXxs(2)` / `radiusXs(4)` 档位，40 个文件 92 处魔法数字机械替换为 `AppTokens`（像素级不变）。
- **UI 优化八项**：窄屏标签隐藏图标只留文字、源管理内层 Tab 可滚动；源列表「未登录」徽章；播放方向默认横屏（桌面 / 平板不强制转向）；播放器 9 个弹层合规化 + 发弹幕横竖屏适配 + 媒体信息 URL 复制按钮 + 播放统计标签可换行；全屏退出残留修复（`edgeToEdge` 统一）；小说主题跟随三档（跟随应用 / 始终夜间 / 始终日间）；**加载态刺眼修复**（阅读器加载指示器加当前阅读背景色底板，夜间不再闪白）；**弹层基准合规首批**（播放器长按倍速、更多菜单、投屏、定时关闭四个弹层补齐滚动与限高）。

### 🖼️ 漫画阅读器缩放交互（专项修复）
- **双击三态**：缩小 → 放大 → 复原循环切换，无需进入设置。
- **触摸板捏合**：支持触控板捏合手势直接缩放。
- **放大后拖动平移与滚轮浏览分离**：放大状态下拖动平移浏览细节，滚轮仍用于翻页浏览，互不干扰。
- **长按与拖动不再冲突**：手势识别层修复，长按唤起菜单不会误触发放大拖动。
- 新增 `docs/UI_BASELINE.md` 设计基准文档（Material 3 + 莫奈动态色体系融合，含窄屏响应式适配规范）。

### 🔧 五项功能修复
- **小说目录实时同步**：详情页与阅读器的目录共用单一数据源（`NovelTocStore`），实时同步。
- **本地导入漫画阅读**：导入的漫画图片目录视为一部漫画，按文件名顺序阅读。
- **本地文件管理**：本地导入的文件支持**删除 / 重命名**（导入历史列表 + 书架本地段均可用，长按 / 右键唤起）。
- **下载路径选择器防冻结**：下载路径设置持久化并立即生效；目录选择器放后台 isolate 执行，**Windows 不再冻结、不再报 0x80070057 / LateInitializationError**。
- **RSS 订阅优化**：订阅先入库、后台补全校验，消除添加订阅时的卡顿。

### 🛠️ 工程修复（重要）
- **恢复误删的平台工程文件**：修复上一版整理 gitignore 时误删的 Windows 构建目录（`windows/`）、Web 平台工程（`web/`）、工具脚本（`tools/`）、弹幕渲染库（`third_party/canvas_danmaku`，vendored MIT）、l10n 产物与测试文件——四平台构建恢复正常。
- **Windows 播放器冻结修复**：`windows/runner/main.cpp` 启用独立 UI 线程策略，修复 window_manager 全屏与引擎线程互锁导致的画面冻结（Flutter issue #176088）。
- **Windows 构建 UTF-8**：CMake 增加 `/utf-8` 编译选项，避免非 ASCII 注释在默认代码页下的 C4819 警告。
- **窗口管理器初始化前置**：`main()` 中 `windowManager.ensureInitialized()` 移至 `runApp` 之前，修复进入 / 退出阅读器画面冻结、F11 / Esc 全屏失效的问题。

### 🎨 应用图标全面更新
- 所有平台启动图标统一替换为 `assets/icon/icon.png` 的新图标（Android mipmap + 自适应图标、iOS / macOS AppIcon、Windows ICO、Web、Linux），并保留自适应图标背景色。

---

### 📝 更新日志（v0.4.2 → v0.4.3）

本版是 v0.4.2 的跟进版，核心是**漫画阅读器的一次系统性重构**：翻页改为「意图驱动」，并对**条漫（Webtoon）回上一话的落点**做了七轮连续修复，彻底解决「停在倒数第二页 / 快速滚动回弹」问题；同时新增常驻捏合缩放与控制栏防误触。

### 📚 漫画阅读器重构
- **意图驱动翻页**：翻页改为「先算目标页，越界直接换章」，修复窄屏末页点击无反应的问题。
- **边界拖拽换章**：在章节边界持续拖拽累计位移后自动切换下一章（松手 / 重新滚动时清零），解决末页无法翻下一章的问题。
- **上一话落点七连修**（条漫）：占位高度 → 判据稳定 → 锚定补差 → `jumpTo` 计算对齐系数，彻底消除「停在倒数第二页」与「快速滚动回弹」：
  - 修复 `fitWidth` 模式未加载图片的占位高度，修正回上一话落点偏移；
  - 修正两处「到底」逻辑误认占位高度为真实高度；
  - 改用「跳到底部 + `maxScrollExtent` 稳定」判据落末页；
  - 收尾改为校验落点重锚，同步恢复期页码显示；
  - 锚定 + 像素补差精准落末页；
  - 最终改用 `jumpTo` 计算对齐系数瞬移落末页，消除快速滚动回弹。
- **常驻捏合缩放**：放大手势常驻——翻页模式下双指捏合可直接放大；单指平移仅放大态生效，未放大时交还底层滚动 / 翻页。
- **控制栏防误触**：点击热区新增控制栏区域保护，点在顶部 / 底部按钮上不触发翻页。
- **章节边界提示**：已是第一 / 最后一章时给出提示文案。

---

### 📝 更新日志（v0.4.1 → v0.4.2）

本版是 v0.4.1 的**紧急修复版**，专门修复**条漫（Webtoon）阅读器**在 v0.4.1 中暴露的两个翻页体验问题。

### 📚 条漫阅读器修复
- **条漫滚到底时当前页锁定末页**：修复窄屏下滚动到底后当前页停在倒数第二页、进度条与章节进度不同步的问题——滚到最底部时当前页正确锁定为末页。
- **条漫末页翻不动时兜底切下一话**：当条漫在末页继续向上翻、滚动被页面夹紧导致翻页失效时，自动兜底切换到下一话，避免「卡在最后一页翻不过去」。

---

### 📝 更新日志（v0.4.0 → v0.4.1）

本版是 v0.4.0 的跟进版，重点是**修复 v0.4.0 在海外 CI 上因 Gradle 仓库镜像顺序导致的构建失败（四平台现已正常出包）**，并强化 Bangumi 同步、重构源管理、补齐年龄限制与一批阅读体验修复。

### 🌸 Bangumi 同步增强
- **详情页一键同步**：条目详情页直接「同步到 Bangumi」，可精确到某一集 / 某一章。
- **手动集 / 章同步**：支持对单集、单章手动触发同步，不再只能全量。
- **播放器单线路可选 + 清理菜单**：剧集按当前线路过滤；播放器新增「清理」菜单（清除观看记录 / 进度等）；同步弹窗按网站样式重做，可一次勾选多集 / 多章批量同步。
- **选线路 → 展示全部线路**：详情页选线路后，播放器展示该源全部线路，批量勾选更顺手。
- **增量同步 + 冲突解决**：云同步改为增量传输，新增冲突检测与状态明细（哪里同步了、哪里冲突了看得清）。
- **统一备份归档**：本地导入 / 导出与云同步重设计，备份包结构统一、可渐进恢复；补齐「记住播放 / 阅读位置」开关。
- **数据安全修正（重要）**：修复云同步 / 本地备份白名单漏掉多个存储盒的问题，避免备份与同步时丢失数据；并补齐 Bangumi 登出后 UI 实时刷新、线路面板无线路时的友好提示、帮助文档链接。
- **阅读 / 进度 / 播放修复**：修复若干阅读进度、播放进度与同步弹窗的体验问题。

### 🧩 源管理重构
- 源库**订阅 / 挑选导入**，不再只能手动粘贴 JSON；源 JSON **全字段可视化编辑**；修复部分源在源管理页的登录态识别与刷新。

### 🔞 年龄限制体系
- 完善的年龄限制策略；**18+ 源默认自动隐藏**（可手动开启）；新增**网络收藏**与**源公告（announcement）**能力。
- **源作者责任**：请在源 JSON 中如实填写 `ageRating` 字段（`general` 全年龄 / `teen` 青少年 16+ / `mature` 成人 18+，支持 `all`/`16`/`r18`/`nsfw` 等别名，缺省 `general`）。应用仅依据该字段自动分级与隐藏，**最终的内容合规性与年龄适宜性由源的提供方与使用方负责**。

### 📚 阅读 / 漫画体验
- 漫画阅读器：修复翻页与进度条不同步。
- 暗色模式阅读体验修复（背景与预览）；阅读器文案 / 预览调整；详情页选集 / 章节目录日期跟随全局日期格式；收起态标题定位修复；Bangumi 评论文档整合。
- **6 项体验问题修复** + 补齐此前轮次未提交的改动。

### 🔧 其他
- 视频嗅探页 release 包一直加载的兜底修复。
- 新增 GitHub Issue 模板（bug / feature / docs）。
- 补齐遗漏文件（content_detail_tabbed_shell 等），确保远端 master 与工作树一致。
- 同步源 JSON 字段规范文档更新至 v0.4.0（新增 `network` / `comments` / `login` / `webFavorite` / `ageRating` 等字段说明）。
- **CI 构建修复**：Android Gradle 仓库改 `google()` / `mavenCentral()` 优先、阿里云兜底，解决海外 runner 拉不到 Google 构件（如 flatbuffers-java）导致构建失败。

---

### 📝 更新日志（v0.3.1 → v0.4.0）

> ⚠️ **这是一次大版本更新**：相对 v0.3.1 变更 **93 个文件、新增约 2 万行代码**，一次性引入六大块新功能并重构了详情页。**本版 Bug 数量预计明显高于以往**，升级前请务必备份数据；追求稳定的用户建议等待 v0.4.x 修补版本。
>
> ✅ **数据兼容**：无破坏性变更。旧收藏 / 进度 / 书签 / 历史可直接沿用；新增的分组、评分、短评均为追加字段；**旧源 JSON 无需修改、无需重新导入**。
>
> ✅ **签名沿用**：与 v0.3.0 / v0.3.1 同一密钥，可直接覆盖安装。
>
> 🗳️ **征求意见**：Bangumi 集成的设计方向（同步方向 / 自动上报 / 绑定策略 / 隐私边界 / 是否扩展到影视）正在 [Discussions](https://github.com/nexhub-app/Nexhub/discussions) 公开征求社区意见，欢迎参与。

本版主题是「**从单机工具走向可同步、可配置、可社交的聚合客户端**」。

### 🌸 Bangumi 集成（核心）
- **两种登录**：OAuth 2.0 授权登录（浏览器授权 → `nexhub://oauth/callback` 深链回跳，带 refresh_token 自动续期）与手填个人 Access Token（保存前校验 `/v0/me`）；凭据存系统安全存储。
- **OAuth 应用凭据编译期注入**（`String.fromEnvironment`），源码不含凭据；CI 已注入，自建构建未注入时 OAuth 按钮提示「未配置」，可改用 Access Token。
- **同步内容**：收藏状态（想看 / 在看 / 看过）、动漫已看集数（增量标记，只标差集）、漫画 / 小说阅读进度（ep_status）、本地评分、短评；可选把本地分组名作为标签推送、新建收藏设为私有。
- **同步策略保守**：以推送为主，仅「本地空缺时回拉」评分 / 短评；状态只进不退、进度只增不减，不覆盖远端「搁置 / 抛弃」。
- **同步粒度**：按模块分别开关（动漫 / 漫画 / 小说）；支持全量同步、详情页单条同步、「从 Bangumi 导入」（按标题相似度自动绑定，不新建本地收藏）。
- **详情页 Bangumi 标签页**：封面、评分与评分人数、五状态收藏统计、简介（展开 / 收起）、集数与放送日期、全部标签、角色、关联作品、吐槽；含「网页打开 / 手动绑定 / 同步状态覆盖」入口。
- **条目绑定**：本地条目 ↔ Bangumi 条目映射；优先缓存，其次标题相似度自动匹配（≥0.85 自动采用），否则给候选列表手选；可解绑、可手动指定收藏状态。
- **代理 / 镜像**：直连 / 镜像两模式，镜像下可分别填主站域名、API 域名、图片域名，解决官方域名不可直连与封面加载失败。

### 🌐 网络配置体系（全局 + 源级）
- **全局可配**：代理（直连 / 系统 / 手动，HTTP 或 SOCKS5，含账号密码，密码单独加密存储）、DNS（系统 / 自定义 UDP / **DoH** / **DoT**，含解析缓存，内置 Cloudflare / Google / Quad9 预设）、自定义 Hosts、SNI、ECH。
- **源级覆盖**：源管理内进入某源的网络覆盖页，按「代理 / DNS / SNI / ECH / Hosts」逐项选择继承全局或单独覆盖。优先级：**用户覆盖 > 源 JSON `network` 块 > 全局 > 默认**。
- **全局生效**：`NexhubHttpOverrides` 使配置覆盖全应用 `dart:io` 流量（含图片加载、下载器）；DNS 解析带 TTL 缓存并优先命中自定义 Hosts；**改动即时生效，无需重启**。
- **内置连通性测试**：代理测试、DNS 测试、DoH 测试。
- ⚠️ **限制**：SNI 受 Dart TLS 栈限制仅尽力生效；**ECH 界面完整但运行时暂不生效**。

### 🗂️ 收藏分组与本地评分
- 一个条目可属**多个分组**（标签式），与站点题材分类相互独立。
- **按模块隔离**：动漫 / 漫画 / 小说分组各自独立，互不串台。
- **可隐藏**：分组可标记隐藏，不出现在分组栏与筛选中，可在「分类管理」恢复。
- **多选**：分组栏可同时选中多个分组做并集筛选；条目分组指定面板为多选覆盖式赋值。
- 书架顶部分组栏 [全部][未分组][各分组]，点击自动居中；管理面板支持拖拽重排、重命名、删除（仅解除关联）、新建。
- **书架筛选新增「分组」维度**（含「未分组」哨兵），与排序 / 分类 / 状态 / 进度叠加。
- **本地评分与短评**：0–10 分 + 短评，入口为详情页 Bangumi 面板与书架卡片长按；可作为 rate / comment 推送 Bangumi。

### 💬 评论功能
- 详情页新增「评论」标签，分「网站评论」（源站）与「Bangumi 吐槽」两个子页。
- **Bangumi 吐槽只读**（公开接口，无需登录）。
- **源站评论可读写**：列表、加载回复、发表、回复、点赞、举报——每项按源 JSON 声明的路由决定，未声明的操作按钮不渲染。
- 源未声明 `comments` 时只显示 Bangumi 吐槽子页。

### 🔑 源登录鉴权
- **网页登录**：内嵌 WebView 登录站点，自动捕获会话 Cookie；**手动粘贴 Cookie** 作为桌面端降级方式。
- 登录态自动识别与刷新（Cookie 变化时重新评估），面板顶部显示当前登录状态。
- 支持**退出登录**：`CookieStore` 新增按 host 删除，仅清除该站点会话，不影响其他源。

### 🧭 详情页重构（三模块统一）
- **删除** `comic_detail_screen.dart` 与 `novel_detail_screen.dart`，漫画 / 小说 / 影视统一走 `content_detail_screen` + `content_detail_tabbed_shell`。
- **Tab 化**：Hero 大图（滚动收起吸顶）+ 浮层主操作（续看 / 续读，首屏可点）+ 吸顶标签栏「详情 / Bangumi / 选集（章节目录）/ 评论 / 推荐」；**选集一次点击直达**（旧版需点开折叠入口或弹窗）。
- 重构后能力**一项未减**。

### 🎨 其他体验
- **统一批量下载选集弹窗**：三模块共用，影视自动按线路去重计总数，修复旧实现的控制器泄漏。
- **骨架屏**：微光加载占位，支持相位错开避免整屏同频闪烁。
- **进度卡片**：新增全部读完 / 看完的完结态高亮，数字改为补间滚动动画。
- 详情页与书架若干窄屏布局修复。

### 🧩 源 JSON 新增字段（面向源作者，全部可选）
| 字段 | 用途 |
| --- | --- |
| `network` | 源级网络覆盖块：`proxy` / `dns` / `hosts` / `sni` / `ech`；缺省继承全局；非法值只告警不阻断启用 |
| `comments.provider` | 默认 `source`；`bangumi` 仅解析、本期未实现 |
| `comments.routes` | 仅 `list` 必需；`replies` / `post` / `reply` / `like` / `report` 可选，未声明则按钮不渲染 |
| `comments.selectors` | `items` / `commentId` / `author` / `avatar` / `content` / `time` / `likeCount` / `replyCount` / `hasMore` / `success` 等，与顶层 selectors 同引擎 |
| `comments.login.url` | WebView 登录页地址 |
| `comments.login.checkCookie` | Cookie 出现该键名即视为已登录 |
| `comments.login.checkUrl` + `loggedInSelector` | 可选二次探测，确认登录有效 |

评论路由支持 `comments.` 命名空间，占位符替换 / 镜像切换 / 相对路径补全与主路由一致。

### 🔧 构建与 CI
- CI 四平台（Android / Windows / Linux / macOS）build 命令补齐 `--dart-define=BANGUMI_CLIENT_ID` 与 `BANGUMI_CLIENT_SECRET`（来自仓库 Secrets），修复官方 Release 包 OAuth 登录不可用的问题。

### 🧪 测试
- 新增 13 个测试文件，覆盖 Bangumi 绑定 / 条目 / 同步服务、评论 API 与 UI、DNS 解析、网络配置与客户端构建、有效网络画像、收藏分组与分组栏、源鉴权管理、HttpFetcher 画像等。

---

### 📝 更新日志（v0.3.0 → v0.3.1）

本版聚焦**视频播放内核与解码健壮性**，补充**中国传统色主题**与**签名管理生产级升级**；同时提升 Android 在部分机型与国内网络下的构建 / 渲染稳定性，并修复若干解析与弹幕匹配问题。

### 🎞️ 视频后端简化
- 移除 `fvp` 视频后端依赖，统一为 **media_kit + media_kit_libs_video（libmpv）** 单一内核路径；清理 fvp 注册与注释（splash / main / platform_service）。

### 📊 新增：播放统计面板
- 新增 `PlayerStats` 模型，读取 mpv 只读属性（实际解码器 `hwdec-current`、编码、像素格式、分辨率、渲染 / 解码掉帧、码率、缓冲进度）。
- `VideoPlayerBackend` 增加 `getProperty`，`MediaKitBackend` 实现读取 mpv 属性。
- 播放器「更多」菜单新增「播放统计」项，展示当前解码方式（软 / 硬解）、编码、像素格式、分辨率、掉帧、码率与缓存缓冲。

### 🛡️ 解码异常自动降级与自愈
- 硬件解码失败（花屏）时，`PlayerController` 自动降级为软件解码并广播 `decodeFallbackStream`；播放页弹出提示并 **re-open 当前地址** 使新解码路径真正生效（mpv 的 hwdec 对已在播解码器不即时生效）。
- 手动切换解码模式同样 re-open 生效；新增「硬件解码+」选项提示「花屏时推荐」。

### 🔐 签名管理生产级升级（安全性）
- 固定签名密钥不再提交入库：`android/app/upload-keystore.jks` 改由 **CI Secret 注入**，密码存放于 `android/key.properties`（不入库，提供 `key.properties.example` 模板）；`.gitignore` 补充密钥临时文件忽略。
- 签名密钥本身与 v0.3.0 完全一致，**从 v0.3.0 仍可覆盖安装**（无需卸载）。

### 💬 弹幕匹配修复
- 修复跨标题误匹配：弹幕库匹配须 `isMatched` 或标题相似，取消盲选回退，避免错配到不相关的弹幕库。

### 🔎 解析 / 嗅探修复
- 笔趣阁类源验证拦截修复 + Cookie 持久化，提升小说源稳定性。
- 视频嗅探稳定性修复（含此前对象库损坏后重建丢失提交的修复）。

### 🤖 Android 渲染修复（花屏）
- `AndroidManifest.xml` 增加 `io.flutter.embedding.android.EnableImpeller=false`，回退 **Skia** 渲染，修复部分旧 GPU / 驱动（Mali、旧 Adreno）在 Impeller OpenGL 后端下视频外纹理**闪烁 / 花屏**的问题（与解码方式无关，软解亦如此）。

### 🔧 Android 构建可靠性（国内网络）
- `build.gradle.kts` / `settings.gradle.kts` 增加阿里云 maven 镜像（google / central）优先、官方源兜底，缓解 `dl.google.com` 直连断流假死。
- `gradle.properties` 增加 Gradle HTTP 连接 / 套接字超时（60s）与最大重试（3 次），避免下载超时无限假死。

### 🎨 主题：6 套中国传统色预设
- `app_tokens.dart` 新增 **茶红 / 桃夭 / 天水碧 / 朱殷 / 群青 / 玄色** 种子色（含中文名，权威值取自 zhongguose.com 与郭浩《中国传统色》）。
- 修复**玄色渲染**：选中玄色时走专属「墨黑 + 赤」主题，修复近黑种子色经 `fromSeed` 生成灰阶导致「无效果」的问题。
- 预设色块**长按 Tooltip 显示中文色名**。

### 🖥️ 界面
- 详情页窄屏响应式布局优化。
- 在线浏览页「布局」按钮重复显示修复，统一布局入口。
- 视频嗅探页提示文字被挤成竖排修复。

### 🧪 测试
- `test/player_screen_test.dart` 新增约 138 行，覆盖播放统计与解码降级 / 手势相关逻辑。

---

### 📝 更新日志（v0.2.13 → v0.3.0）

本版是一次较大的播放器与弹幕体验升级，并新增视频嗅探引擎、统一许可证为 Apache-2.0。相对 v0.2.13 共合并 16 个提交，涉及 40+ 文件。

### ⚖️ 许可证与合规
- 仓库许可证统一规范为 **Apache-2.0**，新增 `NOTICE` 文件并完善第三方致谢署名（canvas_danmaku MIT、Legado、Mihon、RSSHub 等）。
- 致谢章节移除未实际借鉴的 GPL-3.0 仓库脚注，避免许可混淆。
- 「关于」页（about_screen）新增第三方许可 / 致谢展示与 NOTICE 入口。

### 🔎 新增：视频嗅探引擎
- 新增独立**视频嗅探引擎**（`lib/core/sniffer/*`：sniffer_engine / sniffer_bridge / sniffer_models / sniffer_rules）+ 规则文件 `assets/sniffer/sniffer_rules.json`。
- 嗅探页（`browse_sniffer_screen`，626 行）替换旧的 `video_sniffer_screen`：通过 WebView 注入 JS 捕获页面内 m3u8 / 视频直链（含 `URL.createObjectURL`），经桥接回传 Dart；规则自动过滤广告 / 缩略图 / beacon。
- 视频解析改为**嗅探优先**：优先用嗅探结果定位真实播放地址，并修复「拖动进度后重连从头播放」的问题。
- 嗅探结果支持复制到剪贴板 / 保存到 `NexHub/sniffer` 目录，自动带 Referer 避免 403。

### 💬 弹幕系统重构
- **本地化弹幕渲染引擎**：将 canvas_danmaku 引擎 vendor 到 `third_party/canvas_danmaku`（保留 MIT 许可），支持定制渲染。
- **本人发送弹幕改为白色胶囊描边**（原引擎硬编码绿色矩形）：补丁支持圆角胶囊 + 白色描边，区分自己与他人的弹幕。
- 统一**播放器弹幕设置**与**全局弹幕页设置**为单一数据源，避免两套设置互相覆盖；并加双重保险 apply。
- 弹幕设置持久化改为**以文件为主**，退出重进仍保持；修复加载时序导致的「退出重进不保持」。
- 弹幕设置生效修复：字号 / 不透明度 / 自定义颜色实际生效。
- 弹幕设置面板改为**页面内悬浮面板**，修复 Windows 上点开即崩溃。
- 新增**弹幕匹配面板**（`danmaku_match_sheet`）：手动 / 自动匹配弹幕库（DandanPlay）。
- 修复连续打开视频闪退、弹幕设置套用时序、`onReady` 残留、屏幕亮度异步未捕获异常等。

### 🎬 播放器手势与稳定性
- **手势系统重构**（`video_player_screen`，+992 行）：
  - 单击：显隐控制栏。
  - 双击分区：中=播放/暂停，左=快退 10s，右=快进 10s（锁定态忽略）。
  - 长按：切到自定义倍速，松手恢复（受 `longPressSpeedUp` 开关控制，可在「更多」菜单选倍速值）。
  - 左竖滑亮度 / 右竖滑音量 / 横滑 seek 预览 + 中央手势指示器浮层。
  - 键盘：空格播放/暂停，左右 seek ±10s，F 全屏，M 静音。
- **设置生效地基**：全局播放器默认设置（解码 / 音频 / 比例 / 倍速 / 音量 / 连播 / 方向锁定 / seek 倍率 / 字幕样式）加载与应用打通，使「设置页」字段真正即时生效。
- 修复 **HLS 原生崩溃**：退出重进时正确释放 VideoController。
- 修复**视频重连循环**：同实例 re-open 自愈 + seek 宽限期放宽至 60s。
- 修复连续打开视频闪退、弹幕设置套用时序、屏幕亮度异步未捕获异常、`final` 局部变量 speed 延迟赋值编译错误等。

---

### 📝 更新日志（v0.2.12 → v0.2.13）

本版聚焦影视模块的解析与播放稳定性，并顺带纳入 v0.2.12 的 Android 固定签名：

### 🐛 修复 / 稳定性（影视模块）
- **解析层重构与稳定**：重写 `http_fetcher`（重试 / 编码 / 超时更稳），并修复 `builtin_resolver` / `webview_resolver` / `video_extractor` / `media_api_service` 多处解析失败与视频提取异常；WebView 渲染后回灌 HTML 再解析的路径更稳健。
- **播放器退出崩溃修复**：播放器在 `deactivate` 时切断 `stall/position/completed` 流订阅并置 `_disposed` 标记，修复「访问已 deactivated widget 的 ancestor」溢出崩溃；snackBar 退出动画 context 丢失兜底；修复嵌入 `Positioned` 自适应视频区填充。`webview_verification_screen` 验证 / 提取流程更稳定。
- **总集数统计修正**：改为按线路（`Episode.lineName`）分组取最大一组集数，避免多线路镜像集数被累加（如 4 线路 30 集误算 120 集）。
- **选集精确解析**：`fetchEpisodes` 支持透传 `detailUrl`，按详情页地址精确解析选集。

### ✨ 解析引擎增强（源即插件能力）
- **POST 表单路由**：`builtin_resolver` 支持 `routes.X.method:"post"`，自动把查询参数拆成表单体发送，适配 MacCMS 等「分类 / 列表由前端 JS POST 接口填充」的站点（如 `/ds_api/vod`）；纯配置驱动，不写死站点。
- **周期表（周更）解析**：新增 `week` 路由与周列表选择器支持，能从 `status` 解析开播星期并分组展示。
- **分类筛选「各不相同」**：`SourceFilterConfig` 新增 `byCategory`（按分类 id 覆盖筛选组）与 `defaults`（分类默认参数，如 233 动漫 `sort=hits/year=2026`），全部由源 JSON 声明，无需在 Dart 写死。
- **坏链修复**：`video` 路由 `{url}` 直达绝对地址，避免被 base 前缀成双 host 坏链（`https://base/https://episode`）导致播放失败；相对地址也做了 base 规范化避免 `//` 双斜杠。
- **中文搜索 / 筛选修复**：`keyword` 与含中文的筛选占位符自动 `encodeComponent`，修复中文关键词搜索错乱、MacCMS 中文筛选（如「奇幻」）无结果；MacCMS `show` 路由第 1 页 `page` 段留空，避免拼出错误段导致 404。
- **JsonPath 空值保护**：非字符串 selector 增加空值兜底，避免解析崩溃。

### 🎨 UI / 布局
- **周期表统一布局**：改用统一 `ContentCard`，跟随全局布局（网格列数 / 列表模式 / 圆角 / 标题 / 作者），`ListenableBuilder` 实时刷新；星期 Chip 全部可点（空日显示空态）。
- **全局「布局」按钮**：浏览页 AppBar 新增「布局」按钮，首页 / 周期表 / 分类 / 排行榜 Tab 均可改布局。
- **详情页主演可折叠**：影视详情页主演信息块改为可折叠（`_InfoChipsSection`），每组默认显前 N 位，超长显示「展开 N 位 / 收起」，年份始终显示。
- **首页 / 内容列表 / 源搜索 / 章节列表**等稳定性与布局修复（`online_content_list_screen` / `online_home_section` / `module_source_search_screen` / `chapter_list_section`）。
- 新增多语言文案（l10n）。

> 本版已包含 v0.2.12 的固定签名 keystore，Android 包可覆盖安装（更早的随机签名旧包需先卸载一次）。

---

### 📝 更新日志（v0.2.11 → v0.2.12）

本版修复 Android 包因「每次 CI 随机生成签名」导致**无法覆盖安装**的问题：

### 🐛 修复
- **Android 无法覆盖安装**：此前 release 构建使用默认 debug 签名，而 CI 每次都是全新虚拟机、`~/.android/debug.keystore` 不存在，构建工具每次随机生成新 keystore，导致每个 APK 签名证书都不同、旧包无法覆盖。本版生成固定签名 `android/app/upload-keystore.jks`（别名 nexhub，PKCS12，RSA 2048，有效期至 2053 年）并提交入库，`build.gradle.kts` 的 release 构建改用它；并加 `.gitattributes` 把 `*.jks` 标为二进制，防止 Git 检出时换行符转换损坏 keystore。
- ⚠️ **安装注意**：你手机上已装的随机签名旧包无法被换签覆盖，必须**先卸载一次**再装 v0.2.12；之后各版本均可直接覆盖升级。

---

### 📝 更新日志（v0.2.10 → v0.2.11）

本版相对 v0.2.10 **无应用功能变更**，仅为项目补充「致谢」说明：

### 🙏 文档
- **新增致谢章节**：在 README 与发布说明中正式感谢 [Legado](https://github.com/gedoor/legado)（及各类改版）、[Mihon](https://github.com/mihonapp/mihon)（Tachiyomi 衍生版，及各类改版）与 [RSSHub](https://github.com/DIYgod/RSSHub)。
  - Legado 的「书源」规则系统为 NexHub「源即插件」的解析与共创理念提供了核心启发；NexHub 小说分页器以独立 Dart 实现借鉴其 `ChapterProvider` 分页算法（未复制源码），Legado 上游未附许可证，本致谢仅作启发署名。
  - Mihon（Apache-2.0，© Mihon contributors）的扩展源与漫画阅读器交互为 NexHub 的漫画解析与阅读体验提供了重要参考。
  - RSSHub（AGPL-3.0，© DIYgod）为 NexHub 的订阅功能提供 RSS 聚合服务，NexHub 仅作客户端调用，未修改或再分发其源码。
- 仅更新文档与致谢，不涉及任何代码 / 构建产物变更；应用功能与 v0.2.10 完全一致。

---

### 🎨 更新日志（v0.2.9 → v0.2.10）

本版**新增一个用户可见的平台能力**：Android 13+ 的「主题图标(themed icon / 莫奈取色)」——桌面图标会**跟随壁纸颜色**而变色。

### ✨ 新增
- **App 图标莫奈取色（Android 13+ 主题图标）**：之前 `flutter_launcher_icons` 只配了 `adaptive_icon_background` 和 `adaptive_icon_foreground`，**缺一个 single-layer 的单色图层**，所以系统在 Android 13+ 桌面要把图标染成壁纸色时染色层缺席，图标始终是固定的青底白 "N"。本版新增 `assets/icon/icon_monochrome.png`（白"N" + 四角白色装饰点，透明背景），并在 `pubspec.yaml` 的 `flutter_launcher_icons` 块里挂上 `adaptive_icon_monochrome`。下次 CI 重新跑 `flutter_launcher_icons` 时，Android job 会一并产出 `ic_launcher_monochrome` 资源，桌面图标就会按系统壁纸色取色（青底不变作为品牌色，"N"+ 装饰点跟随壁纸变色）。
- 仅影响支持主题图标的 Android 13+ 设备；老设备、Android 其它版本以及 Windows/macOS/Linux 完全不受影响（图标显示行为照旧）。

---

### 📝 更新日志（v0.2.0 → v0.2.9）：多平台安装包 / 图标 / 签名修复（无应用功能变更）

v0.2.1 至 v0.2.9 这一阶段**没有应用功能变动**，集中修复各平台安装包的打包、图标与签名问题（为避免重复，合并记录在此）：

- **Windows 自定义启动图标**：`flutter_launcher_icons` 重新生成图标，安装包 / 解压包均带 NexHub 官方图标（此前是 Flutter 默认 Logo）。
- **Windows exe 安装包**：改用 Chocolatey 装 Inno Setup 后直接调 `ISCC.exe` 编译 `windows/installer/NexHub.iss`，去除失效的第三方 action 依赖；并依次修复 `SetupIconFile` 路径多写一层 `..\..`、随仓库提交官方 `ChineseSimplified.isl` 语言包并改用相对仓库根的正确路径、`OutputDir` 输出目录错位（ISCC 写到仓库根 `Output/` 而非 `windows/installer/Output/`）、以及 CI「复制兜底」把已在目标位的 exe 再复制到自己身上导致的 `with itself` 自覆盖报错（加绝对路径比较守卫）。
- **macOS / Linux 应用图标**：CI 在 build 前重生成 `AppIcon.appiconset`；Linux 用 `flutter create --platforms=linux .` 重建工程后覆盖图标；清理仓库里无关的视频播放占位图（带"图片由AI生成"水印）。
- **致谢补充**：此阶段在 README / 发布说明补充了对 Legado / Mihon / RSSHub 的致谢（详见 v0.2.10 前的说明）。

---

### 📝 更新日志（v0.1.0 → v0.2.0）

本版相对 v0.1.0 共合并 22 个提交，重点如下：

### 🐛 关键修复
- 修复全局页面转场淡出方向写反导致整页隐形（全局黑屏）的问题。
- 视频内核初始化失败时不再拖垮整个应用（黑屏兜底：仅视频不可用，浏览 / 阅读照常）。
- 新增全局错误兜底：构建期异常显示可读红字而非黑屏，便于定位根因。

### ✨ 新功能 / 体验
- 漫画阅读器：支持鼠标拖拽 / 滑动翻页、滚轮「作用 / 方向」设置、条漫缩放与翻页。
- 封面图 Hero 共享元素：从列表卡片平滑飞入详情页。
- 弹弹 play 弹幕改为编译期注入，并移除凭据页（密钥经 CI Secrets 注入）。

### 💫 灵动动效（全套升级）
- 底栏灵动动效 + 主题色胶囊指示。
- 统一灵动动效原语：按压回弹（AppTapScale）、入场（Entrance）、弹窗 / 抽屉回弹（AppSheetBody）。
- 全局页面切换转场最终改为零动画瞬切，消除返回时的定格卡顿。
- 列表项 / 卡片灵动入场；图标按钮与播放器控制按压回弹。
- 加载指示器升级为三点弹跳；下拉刷新弹性；顶栏滚动渐隐。
- 内容卡片按压回弹 + 对话框弹性弹出。
- 性能优先：去除唯一不必要的无限循环动画（空闲零开销）。

---

## 📦 安装

在下方 **Assets** 中下载对应你系统的安装包：

| 系统 | 文件 | 安装方式 |
| --- | --- | --- |
| **Android** | `app-arm64-v8a-release.apk`（绝大多数手机）/ `app-armeabi-v7a-release.apk`（老机型）/ `app-x86_64-release.apk`（模拟器） | 手机允许「未知来源」安装后，点击对应架构的 APK 安装。装错架构会提示「安装包无效」 |
| **Windows** | `NexHub-setup-x.y.z.exe`（推荐）/ `NexHub-windows-x64.zip` | 双击 exe 按向导安装；或解压 zip 后运行里面的 `NexHub.exe` |
| **Linux** | `NexHub-linux-x64.tar.gz` | 解压后运行 `bundle/nexhub` |
| **macOS** | `NexHub-macos.zip` | 解压后将 `NexHub.app` 拖入「应用程序」文件夹 |

> ⚠️ macOS 包**未公证**，首次打开可能需要在「系统设置 → 隐私与安全性」中点击「仍要打开」。

## 🔌 重要：先导入源

本应用**不内置任何源**，首次打开是空的，这是正常现象。请先导入至少一个源 JSON（应用内「源管理 / 导入源」），才能看到内容。源由社区共享维护，请通过社区渠道获取，并自行评估其合规性与内容合法性。

> 完整的使用与开发指南见仓库 [README](https://github.com/nexhub-app/nexhub#readme)。

## ⚠️ 免责声明

NexHub 是一款开源技术演示项目，本身**不提供、不存储、不中转任何受版权保护的内容**，也**不内置任何内容源**。所有内容均来自用户自行导入的源，其合法性与版权状况由源的提供方与使用方负责。使用本软件即表示你已阅读、理解并同意仓库内完整的《免责声明》（依据中华人民共和国相关法律法规拟定）。

## 🙏 致谢

感谢 [Legado](https://github.com/gedoor/legado)（及各类改版）与 [Mihon](https://github.com/mihonapp/mihon)（Tachiyomi 衍生版，及各类改版）——前者「书源」规则系统启发了 NexHub「源即插件」的解析与共创理念，后者的扩展源与漫画阅读器交互为漫画解析与阅读体验提供了重要参考。也感谢 [Flutter](https://flutter.dev) / Dart 团队，以及 [flutter_js](https://pub.dev/packages/flutter_js)、[media_kit](https://pub.dev/packages/media_kit)、[canvas_danmaku](https://pub.dev/packages/canvas_danmaku) 等开源项目，和所有为「源即插件 · 共创社区」理念做出贡献的开发者与源作者。

## 📝 已知限制

- **iOS 安装包暂未提供**：需 Apple 开发者账号签名，请在本机执行 `flutter build ios` 自行打包。
- **macOS 包未公证**：跨设备分发可能受系统拦截。
- **安卓 APK 签名密钥（经 CI Secret 注入）**：release 包的固定签名密钥**不再提交入库**，改由 **GitHub Actions Secret 注入**，`android/key.properties` 仅存密码（不入库，提供 `key.properties.example` 模板），符合生产级安全要求。密钥本身与 v0.3.0 一致，**从 v0.3.0 起可覆盖安装**；若你仍停留在更早的随机签名旧包，则需先卸载一次。若要上架应用商店，请改用你自己的私有正式签名密钥。
- **基础功能已完成**：后续版本以修复 bug 与功能进阶为主，欢迎通过 Issue / 社区反馈问题。
- **ECH 暂不生效**：网络设置中的 ECH 界面已就绪，运行时尚未接通；**SNI 仅尽力生效**（受 Dart TLS 栈限制）。
- **自建构建的 Bangumi OAuth 不可用**：应用凭据经编译期注入，未注入时请改用 Access Token 登录，或自行到 [bgm.tv/dev/app/create](https://bgm.tv/dev/app/create) 注册应用（回调地址 `nexhub://oauth/callback`）。
- **Bangumi 吐槽仅只读**；源站评论的可用操作取决于源 JSON 声明了哪些路由。
